<?php
namespace Database\Seeders;
use Illuminate\Database\Seeder;
class DatabaseSeeder extends Seeder {
    public function run(): void {
        // Roles are installed by the identity migration. No production demo accounts.
    }
}
