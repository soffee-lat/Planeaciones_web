# Tareas y evidencia

Fecha: 2026-09-07. Fase 0 documental revisada con iteración curricular/comercial y PostgreSQL; implementación no iniciada. Esta entrega se detiene en arquitectura conforme al alcance previo al código. Los checks describen trabajo efectivamente realizado, no funcionalidades existentes.

## Fase 0 — Diseño

- [x] Leer especificación y documentos existentes; detectar conflicto de alcance.
- [x] Preservar PLAN/DATABASE/TASKS originales en docs/legacy.
- [x] Definir arquitectura, módulos, entidades/relaciones, estados y permisos.
- [x] Diseñar tres paneles, IA, archivos, queues, riesgos y exclusiones.
- [x] Definir consumo, idempotencia, correcciones, versiones y margen.
- [x] Consultar documentación oficial de Laravel 13 y Filament 5.
- [x] Revisar coherencia documental y dividir fases pequeñas verificables.

Evidencia: ARCHITECTURE.md, DATABASE.md, WORKFLOWS.md, PERMISSIONS.md, AI_PIPELINE.md, MVP.md y este archivo; PLAN.md como índice. Revisión estática; no instalación ni pruebas de aplicación. La fase anterior de administrador único quedó sustituida, no implementada.

## Fase 1 — Base y acceso

- [x] Verificar PHP/extensiones, Composer, Node, pdo_pgsql y PostgreSQL; resolver y fijar dependencias.
- [x] Instalar Laravel/Filament, tres PanelProviders, español, UTC y zona de negocio.
- [x] Crear identidad/roles, registro, correo, reset, onboarding y Policies base.
- [x] Configurar pruebas PostgreSQL, factories ficticias, queues y storage privado.
- [x] Probar login, roles, acceso cruzado y correo no verificado; revisar UI responsive.

Salida: tres accesos aislados con suite de autorización pasando; lockfiles y .env.example sin secretos.

### Evidencia Fase 1 — 2026-09-08

Verificado por el agente de cierre, sin iniciar Fase 2.

- Runtime: PHP 8.4.7 con `tools/php.ps1` cargando `.runtime/php.ini` que habilita `pdo_pgsql` y `pgsql`; Laravel 13.31.0; PostgreSQL local escuchando en `127.0.0.1:55432` (base `planeaciones` / `planeaciones_test`, usuario `planeaciones`). `composer.lock` y `package-lock.json` presentes; `.env.example` sin secretos y `.env.testing` con credencial local aislada.
- Config: `config/database.php` con `pgsql` como única conexión activa; `config/queue.php` con `database` por default; `config/filesystems.php` con disco `private` en `storage/app/private` y `serve => false`; locale `es`, timezone UTC. Tres `PanelProvider` (`AppPanelProvider`, `ReviewPanelProvider`, `AdminPanelProvider`) montan `/app`, `/review`, `/admin` sobre `BasePanelProvider` que aplica `emailVerification`, `emailChangeVerification`, `databaseTransactions`, middleware CSRF/sesión y `EnsureActivePanelAccess`.
- Identidad: migración `2026_09_08_000001_add_identity_and_roles.php` añade `roles`, `role_user` (PK compuesta) y columnas `status`+`onboarding_completed_at`. `User` implementa `FilamentUser` y `MustVerifyEmail`; `RoleCode` enum (Customer/Reviewer/Administrator). Registro público sólo desde `/app` vía `RegisterCustomer`; reset y verificación cubiertos por `RequestPasswordReset`, `EditProfile`, `Login` (normaliza email). Onboarding en `App\Filament\App\Pages\Onboarding` con acción `CompleteOnboarding`. `UserPolicy` gobierna `view`/`update`/`completeOnboarding`.
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit --testdox` → `OK (28 tests, 142 assertions)` en 9.63 s. Cubre matriz de roles, rechazo de panel cruzado, verificación de correo obligatoria, suspensión y revocación aplicadas a sesión activa, registro público que ignora campos privilegiados, unicidad case-insensitive de email en PostgreSQL, unicidad del pivot `role_user`, CHECK sobre `status`, worker real sobre cola `database`, `afterCommit` en dispatch, row lock entre conexiones y storage privado sin ruta pública ni symlink.
- Revisión visual `/app`: reportada por el agente previo (dashboard, onboarding y responsive móvil del cliente). No re-verificada en este cierre.
- Revisión visual `/review`: servidor `php artisan serve` en `127.0.0.1:8000`. `/review/login` renderiza en español con marca `Planeaciones · Revisión` (acento morado) y enlace de reset. Autenticado, `/review` muestra topbar, avatar, sidebar `Inicio`, título `Espacio de revisión` y las dos secciones placeholder `Hola, {nombre}` y `Tu cuenta, bajo tu control` con botón a `/review/profile`. Contenido operativo intencionalmente ausente (Fases 5–6).
- Revisión visual `/admin`: `/admin/login` con marca `Planeaciones · Administración` (acento ámbar). Autenticado, `/admin` muestra topbar, sidebar `Inicio`, título `Administración` y las mismas dos secciones placeholder apuntando a `/admin/profile`. Sin recursos CRUD expuestos (Fases 2–8).
- Responsive: layout Filament v5 sirve las mismas vistas; NO se ejecutó verificación explícita en viewport móvil sobre `/review` y `/admin` en este cierre (queda como pendiente menor, ver más abajo). El responsive de `/app` fue revisado por el agente previo.
- Aislamiento: la matriz de roles y los rechazos de panel cruzado están cubiertos por `IdentityAccessTest::test_role_access_matrix_is_enforced_on_server`, `test_login_accepts_customer_and_rejects_wrong_panel_and_password`, `test_suspended_and_roleless_accounts_are_denied` y `test_unverified_accounts_cannot_open_any_dashboard`; y por `AccountSecurityTest::test_revoked_role_and_suspension_apply_to_existing_session` y `test_customer_dashboard_contains_no_other_customer_data`. Todos verdes en la corrida actual.
- Archivos modificados en este cierre: `TASKS.md` (esta sección de evidencia). No se tocó código de aplicación; no se corrigió ningún fallo porque la suite ya estaba en verde. Se creó y eliminó un archivo auxiliar temporal `storage/framework/seed_demo_users.php` para poblar dos usuarios demo (`admin@test.local`, `reviewer@test.local`) usados solo en la revisión visual local; no queda commiteado.
- Pendientes menores dentro del alcance de Fase 1: (a) verificación explícita de responsive en viewport móvil para `/review` y `/admin` (Filament es responsive por defecto pero no se probó a mano en este cierre); (b) documentar en README/DEV el uso de `tools/php.ps1` como envoltorio PHP obligatorio para trabajar en Windows con la Postgres local. Ambos no bloquean el cierre lógico de Fase 1.
- Fase 2 NO iniciada. No se creó ninguna migración, modelo, servicio o acción del catálogo curricular ni del importador; los checks de la sección Fase 2 permanecen en `[ ]`.

### Evidencia adicional Fase 1 — cierre pendientes 2026-09-08

- Responsive `/review` a 390×844 y 375×812 (Playwright viewport, `.runtime/review-390.png`, `.runtime/review-375.png`): topbar con hamburger a la izquierda + avatar a la derecha, sidebar oculta accesible por overlay, título `Espacio de revisión`, dos secciones apiladas verticalmente sin scroll horizontal, botón `Ver mi perfil` con tamaño táctil correcto. Sin defectos.
- Responsive `/admin` a 390×844 y 375×812 (`.runtime/admin-390.png`, `.runtime/admin-375.png`): equivalente a `/review`, con título `Administración` y marca `Planeaciones · Administración` al desplegar el sidebar. Overlay del sidebar verificado interactivamente. Sin defectos.
- Como no hubo problemas responsive reales, no se modificó código de la aplicación; ninguna corrección era necesaria. Los PNG están en `.runtime/` (fuera de Git) como respaldo local del reviewer.
- README actualizado con la sección "Desarrollo local en Windows" que documenta `tools/php.ps1` como envoltorio PHP obligatorio (porque el PHP global no trae `pdo_pgsql`) y ejemplos de ejecución para pruebas, artisan y composer.
- Suite completa re-ejecutada tras los cambios documentales: `OK (28 tests, 142 assertions)` en 10.1 s.
- Fase 2 continúa sin iniciar; ningún archivo bajo `app/`, `database/migrations`, `database/factories`, `app/Actions` o `app/Services` fue creado o modificado en este subcierre.

## Fase 2 — Grupos y borradores

- [x] Catálogo curricular: ocho entidades, integridad por versión, publicación inmutable y administración mínima.
- [x] ImportCurriculumDraft/CurriculumImportService y comando administrativo conceptual curriculum:import: JSON/CSV con schema versionado según CURRICULUM.md, autorización y creación exclusiva de nuevo borrador.
- [x] Validar todas las entidades/relaciones, códigos duplicados, referencias inexistentes y destino; transacción todo-o-nada, resumen/errores por ubicación y dry-run sin escrituras; nunca publicar ni sobrescribir versiones.
- [ ] Probar equivalencia JSON/CSV, schema inválido, duplicados, referencias/fases incompatibles, permisos, versión publicada/borrador existente, rollback intermedio, concurrencia y dry-run; fixtures ficticias y publicación editorial separada.
- [ ] Seeders DEMO diseñados en CURRICULUM.md, solo local/test; no cargar currículo real automáticamente.
- [x] Escuelas/grupos/perfil con grade_id curricular y propiedad validada (Subfase 2B). `preferred_format_id` reservado como columna nullable sin FK; su integración con `institutional_formats` queda documentada como pendiente para la fase de formatos.
- [x] NUEVA PLANEACIÓN rápido/avanzado, CurriculumSuggestionService determinista, selección y confirmación con fingerprint.
- [x] Solicitud con pivotes curriculares, snapshot textual completo por revisión y perfil reutilizado (Subfase 2C — base de dominio; NUEVA PLANEACIÓN wizard queda pendiente).
- [x] Probar inmutabilidad incluso al agregar descendientes, mezcla de versiones/grado/fase, propuesta obsoleta/sin coincidencia y acceso a sugerencia ajena (parcial: cubierto para snapshots y pivotes curriculares en 2C; sugerencia y wizard pendientes).
- [ ] Upload en cuarentena, validación y descarga privada autorizada.
- [ ] Probar ownership anidado, formatos/tamaño y ausencia de recaptura.
- [ ] Medir solicitud con perfil completo y corregir fricción.

Salida: captura aislada y reutilizable e importación estructurada validada hacia borrador, sin publicación automática; aún no procesar solicitudes sin derechos.

### Evidencia Subfase 2A — 2026-09-09

Solo se implementó la primera viñeta ("Catálogo curricular"). El resto de la Fase 2 sigue pendiente.

- Migración: `database/migrations/2026_09_09_000001_create_curriculum_catalog.php` crea las 8 tablas (`curricula`, `curriculum_versions`, `educational_phases`, `grades`, `formative_fields`, `curricular_contents`, `pdas`, `articulating_axes`) con claves compuestas `(id, curriculum_version_id)`, FK compuestas para bloquear mezcla entre versiones, y `UNIQUE(curriculum_version_id, code)` por entidad. `curricula.selectable_version_id` como FK circular añadida al final. Triggers PL/pgSQL: `curriculum_element_immutability_guard` (bloquea INSERT/UPDATE/DELETE en hijos si la versión padre está publicada y evita cambiar de versión), `curriculum_version_immutability_guard` (bloquea modificar/borrar versiones publicadas; exige `published_by` y `checksum` no vacío al pasar de borrador a publicado), `curriculum_selectable_version_guard` (rechaza apuntar a versión inexistente, de otro currículo o borrador).
- Modelos + factories: `app/Models/{Curriculum,CurriculumVersion,EducationalPhase,Grade,FormativeField,CurricularContent,Pda,ArticulatingAxis}.php` con relaciones, casts y helpers `isPublished/isDraft` en `CurriculumVersion`. Factories con etiqueta "Datos ficticios, sin validez curricular." y consistencia cruzada (p. ej. `PdaFactory` genera el grado en la misma fase del contenido).
- Publicación: `app/Actions/Curriculum/PublishCurriculumVersion.php` como único camino autorizado. Bloquea la fila raíz de `curricula` y luego la de `curriculum_versions` con `FOR UPDATE`, valida el árbol (entidades no vacías, todo contenido con al menos un PDA, PDA en fase del contenido), calcula un `checksum` SHA-256 sobre el árbol ordenado y marca `published_at`/`published_by`. Lanza `RuntimeException` con códigos semánticos (`CURRICULUM_VERSION_EMPTY_*`, `CURRICULUM_CONTENT_WITHOUT_PDA:codes`, `PDA_GRADE_PHASE_MISMATCH:codes`, `CURRICULUM_VERSION_ALREADY_PUBLISHED`).
- Policies: `AuthorizesCurriculumTree` trait (lectura para cualquier rol activo verificado; escritura sólo Administrator). `CurriculumPolicy`, `CurriculumVersionPolicy` (`update`/`delete`/`publish`/`editTree` sólo si es borrador) y `CurriculumElementPolicy` abstracta reutilizada por las 6 entidades hijas. Auto-descubrimiento de Laravel las asocia por convención.
- Filament admin: recursos `App\Filament\Resources\Curricula\CurriculumResource` y `App\Filament\Resources\CurriculumVersions\CurriculumVersionResource`, registrados en `AdminPanelProvider->resources([...])`. El formulario de currículo restringe `selectable_version_id` a versiones publicadas del mismo currículo (Select con options dinámicas). La tabla de versiones muestra badge "Publicada", `EditAction` y `DeleteAction` ocultas si la versión no es borrador, y `Action::make('publish')` que invoca la acción y traduce errores a notificaciones.
- Seeder DEMO: `database/seeders/CurriculumDemoSeeder.php` (sólo local/testing) crea el currículo `DEMO` con 2 versiones — `DEMO-1` publicada y `DEMO-2` borrador (mismo esqueleto, un `full_text` de contenido cambiado para probar que la publicada se preserva). Verificado con `.\tools\php.ps1 artisan db:seed --class=CurriculumDemoSeeder` y `db:show --counts`: `curricula=1, curriculum_versions=2, educational_phases=4, grades=8, formative_fields=4, curricular_contents=8, pdas=16, articulating_axes=4`. `curriculum.selectable_version_id` apunta a la versión publicada.
- Pruebas nuevas (`tests/Feature/CurriculumCatalogTest.php`, 15 casos, 22 aserciones): publicación válida marca metadata y checksum de 64 caracteres; publicación rechazada por árbol vacío, por contenido sin PDA (`CURRICULUM_CONTENT_WITHOUT_PDA:CT-ORPHAN`), y por PDA en fase incorrecta (`PDA_GRADE_PHASE_MISMATCH`); tras publicar, INSERT/UPDATE/DELETE en hijos y UPDATE en la propia versión lanzan `CURRICULUM_VERSION_PUBLISHED`; grado que apunta a fase de otra versión falla por FK compuesta; `UNIQUE(curriculum_version_id, code)` bloquea códigos duplicados; `selectable_version_id` acepta una versión publicada del mismo currículo y rechaza borrador (`SELECTABLE_VERSION_NOT_PUBLISHED`) o versión de otro currículo (`SELECTABLE_VERSION_MISMATCHED_CURRICULUM`); administrador entra a `/admin/curricula` y `/admin/curriculum-versions` (200), cliente y revisor reciben 403.
- Suite completa post-cambios: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit --testdox` → `OK (43 tests, 164 assertions)` en 14.5 s. Sin regresiones sobre los 28 casos previos.
- Subfase 2B NO iniciada. No se creó `ImportCurriculumDraft`, `CurriculumImportService`, el comando `curriculum:import`, escuelas/grupos/perfil, NUEVA PLANEACIÓN, sugerencias, solicitudes, uploads ni ninguna migración adicional; los checks restantes de Fase 2 permanecen en `[ ]`.

### Evidencia Subfase 2A.2 — 2026-09-09

Solo se implementó la importación estructurada JSON del catálogo curricular (segunda y tercera viñeta de Fase 2). CSV, escuelas, grupos, perfiles, solicitudes, planes, pagos, IA, documentos y revisores siguen pendientes.

- Contrato JSON definitivo: documentado en [CURRICULUM.md](CURRICULUM.md) sección "Contrato JSON definitivo — Subfase 2A.2 (schema_version = 1)". Objeto raíz obligatorio con `schema_version`, `curriculum`, `version` y arreglos no vacíos `educational_phases`, `grades`, `formative_fields`, `curricular_contents`, `pdas`, `articulating_axes`. Referencias por `phase_code`, `field_code`, `content_code`, `grade_code`. Códigos únicos por colección y case-sensitive. `source_reference` acepta string u objeto (se serializa a JSON en la columna).
- Servicio: [app/Services/Curriculum/CurriculumImportService.php](app/Services/Curriculum/CurriculumImportService.php) con `SUPPORTED_SCHEMA_VERSION=1`. Fases: (1) autorización del actor, (2) validación estructural (tipos, requeridos, longitudes 1..64 códigos / 1..1024 nombres / 1..20000 textos, fechas YYYY-MM-DD, effective_until ≥ effective_from), (3) validación referencial (unicidad, resolución de códigos, fase de grado coincide con fase de contenido), (4) transacción PostgreSQL única que crea/reutiliza `Curriculum` (sin modificar metadata), rechaza `(curriculum_id, number)` duplicado con `VERSION_EXISTS`/`VERSION_PUBLISHED`, e inserta el árbol vía modelos Eloquent respetando triggers y FK compuestas. En dry-run o error se lanza una excepción anónima interna que fuerza rollback total; el catch la distingue del error real por mensaje vacío.
- Reportes: [ImportReport](app/Support/Curriculum/ImportReport.php) y [ImportError](app/Support/Curriculum/ImportError.php) exponen `success`, `dry_run`, `curriculum_code`, `version_number`, `draft_id` (solo tras commit real), `file_hash` SHA-256, `counts` por entidad, `errors[]` con `code/message/location` (JSON pointer) y `warnings[]` (p. ej. contenido sin PDA — permite crear borrador pero avisa que la publicación fallará).
- Acción: [app/Actions/Curriculum/ImportCurriculumDraft.php](app/Actions/Curriculum/ImportCurriculumDraft.php) carga el archivo, calcula SHA-256, parsea JSON con `JSON_THROW_ON_ERROR` y delega al servicio. Errores de parseo producen `INVALID_JSON` en el reporte.
- Comando: [app/Console/Commands/ImportCurriculumCommand.php](app/Console/Commands/ImportCurriculumCommand.php) → `php artisan curriculum:import <archivo> --actor=<email|id> [--dry-run]`. Resuelve el actor por email (case-lower) o ID, verifica rol Administrator + status active. Emite reporte legible con conteos, warnings y errores; exit code 0 sólo si (importación real exitosa) o (dry-run sin errores).
- Idempotencia (create-only): la reimportación exacta del mismo archivo falla con `VERSION_EXISTS` (borrador previo) o `VERSION_PUBLISHED` (versión ya congelada). No hay merge, upsert ni sobrescritura. Para corregir se ajusta el archivo y se importa con otro `version.number`, o se elimina el borrador previo por acción editorial separada. Documentado explícitamente en CURRICULUM.md.
- Plantilla de carga real: [docs/curriculum_import_template.example.json](docs/curriculum_import_template.example.json) con el esqueleto de `MX-NEM-PRIMARY` (3 fases F3/F4/F5, 6 grados G1..G6, 4 campos, 7 ejes) y contenidos/PDA como marcadores `<pendiente>`. Uso previsto: el equipo editorial completa `full_text` y `source_locator` desde los PDF SEP antes de importar. NO se importó currículo oficial en este subcierre.
- Diferencias frente al borrador `curriculum_import_contract_draft.json`: `note` raíz se ignora (no está en el schema); `source_reference` como objeto se acepta pero se serializa a la columna VARCHAR; los ejemplos `contents`/`pdas` del borrador eran incompletos y se sustituyen por el esqueleto de plantilla con todas las fases/campos/grados listados. Detallado al final de la sección del contrato en CURRICULUM.md.
- Tests nuevos: [tests/Feature/CurriculumImportTest.php](tests/Feature/CurriculumImportTest.php) — 16 casos / 48 aserciones. Cubre: dry-run válido sin escritura; importación válida crea borrador con `published_at`/`published_by`/`checksum` NULL; currículo existente se reutiliza y su metadata no se modifica; reimportación del mismo archivo → `VERSION_EXISTS` y no duplica; reimportación sobre versión publicada → `VERSION_PUBLISHED`; `schema_version=99` → `SCHEMA_VERSION_UNSUPPORTED`; datos vacíos → `MISSING_FIELD`; JSON inválido → `INVALID_JSON`; código duplicado en el archivo → `DUPLICATE_CODE`; `phase_code` inexistente → `REFERENCE_NOT_FOUND`; PDA con fase de grado ≠ fase de contenido → `PDA_GRADE_PHASE_MISMATCH`; error fuerza rollback sin registros parciales; importación nunca toca `selectable_version_id` ni publica; comando Artisan en modo dry-run y real; comando rechaza actor no administrador.
- Suite completa post-cambios: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit --testdox` → `OK (59 tests, 212 assertions)` en 15.7 s. Sin regresiones sobre los 43 previos (28 baseline Fase 1 + 15 Subfase 2A.1 + 16 nuevos 2A.2).
- Subfase 2B NO iniciada. No se creó escuelas, grupos, perfiles pedagógicos, `curriculum_version_id`/`grade_id` en grupos, `preferred_format_id`, NUEVA PLANEACIÓN, `CurriculumSuggestionService`, solicitudes ni sus pivotes, snapshot textual, uploads, planes, pagos, IA, documentos ni revisores. No se cargó currículo oficial (la plantilla queda como marcadores para revisión editorial).

### Evidencia Subfase 2B — 2026-09-10

Solo se implementó la viñeta "Escuelas/grupos/perfil". NUEVA PLANEACIÓN, `CurriculumSuggestionService`, solicitudes/snapshots, uploads, planes, pagos, IA, documentos y revisores siguen pendientes (Subfases 2C y 2D **NO iniciadas**).

- Migración: [database/migrations/2026_09_10_000001_create_schools_groups_profiles.php](database/migrations/2026_09_10_000001_create_schools_groups_profiles.php) crea `schools`, `groups`, `group_profiles` con integridad server-side. `schools`: PK id, `owner_id` FK users cascade, `school_type` CHECK IN ('public','private'), UNIQUE(id,owner_id) para FK compuesta. `groups`: PK id, `UNIQUE(id, owner_id)`, FK compuestas `(school_id, owner_id) → schools(id,owner_id)` y `(grade_id, curriculum_version_id) → grades(id,curriculum_version_id)`, más FK `curriculum_version_id → curriculum_versions`. Trigger PL/pgSQL `groups_curriculum_version_guard` (BEFORE INSERT/UPDATE de `curriculum_version_id`): rechaza versión borrador con `GROUP_CURRICULUM_VERSION_NOT_PUBLISHED` y versión publicada que no sea la `selectable_version_id` de su currículo con `GROUP_CURRICULUM_VERSION_NOT_SELECTABLE`. `group_profiles`: PK, `group_id` UNIQUE (relación 1:1), `revision` unsignedInteger default 0, CHECKs `student_count BETWEEN 1 AND 200` y `session_minutes BETWEEN 15 AND 480`. `preferred_format_id` unsignedBigInteger nullable SIN FK — la FK a `institutional_formats` se añadirá cuando exista dicha tabla.
- Modelos + factories: [app/Models/School.php](app/Models/School.php) (cast `school_type` → `SchoolType`), [app/Models/Group.php](app/Models/Group.php) (scopes `active/archived`, helper `isArchived`, tabla explícita `groups`), [app/Models/GroupProfile.php](app/Models/GroupProfile.php) (constantes `PEDAGOGICAL_FIELDS` y `REQUIRED_FOR_COMPLETENESS`, helper `isSufficient`). Enum [app/Enums/SchoolType.php](app/Enums/SchoolType.php) con casos `public`/`private`. Factories: [SchoolFactory](database/factories/SchoolFactory.php), [GroupFactory](database/factories/GroupFactory.php) (sin defaults de FK — el caller debe armar la escena válida), [GroupProfileFactory](database/factories/GroupProfileFactory.php) con estado `empty()` para tests de completitud.
- Policies: [SchoolPolicy](app/Policies/SchoolPolicy.php), [GroupPolicy](app/Policies/GroupPolicy.php), [GroupProfilePolicy](app/Policies/GroupProfilePolicy.php) con deny-by-default: cliente activo verificado sólo ve/modifica sus propios recursos por comparación `owner_id === auth()->id()`; administrador tiene lectura; revisor **no** tiene acceso operativo en 2B. `GroupPolicy::archive` separada de `delete`. `GroupProfilePolicy::delete` siempre `false` (perfil se conserva histórico).
- Acción: [app/Actions/Pedagogy/UpdateGroupProfile.php](app/Actions/Pedagogy/UpdateGroupProfile.php) valida rangos (`student_count 1..200`, `session_minutes 15..480`, textos ≤ 8000 chars), toma `lockForUpdate`, compara valor previo vs nuevo por cada campo de `PEDAGOGICAL_FIELDS` y sólo incrementa `revision` si al menos un campo cambió realmente. Autoriza con `Gate::update`. Guardar sin cambios NO incrementa (validado por test).
- Onboarding: [app/Services/Onboarding/OnboardingProgress.php](app/Services/Onboarding/OnboardingProgress.php) expone `isPedagogicalComplete(User)` que verifica (a) al menos una escuela propia, (b) al menos un grupo propio no archivado, (c) `GroupProfile::isSufficient()` sobre `student_count`, `general_level`, `session_minutes`, `characteristics`. Se añadió `User::hasPedagogicalOnboardingComplete()`. Decisión documentada: NO se sobrescribe `users.onboarding_completed_at` (Fase 1 = identidad); el progreso pedagógico se computa como estado derivado para evitar inconsistencias. El dashboard `/app` muestra CTAs distintos según el estado.
- Filament `/app`: [SchoolResource](app/Filament/App/Resources/Schools/SchoolResource.php) y [GroupResource](app/Filament/App/Resources/Groups/GroupResource.php) registrados en `AppPanelProvider::resources([...])`. `getEloquentQuery()` filtra por `owner_id = auth()->id()`. `CreateSchool`/`CreateGroup` fuerzan `owner_id` desde sesión y descartan cualquier valor recibido; `EditSchool`/`EditGroup` eliminan `owner_id` del payload y `EditGroup::resolveRecord()` re-filtra por `owner_id` como defensa adicional. El selector de currículo del grupo lista sólo `curriculum_versions.id` referenciadas por `curricula.selectable_version_id`; el selector de grado depende de `curriculum_version_id` con `Select::live()` + `afterStateUpdated` que invalida el grado si deja de pertenecer a la versión. Los selects muestran nombres amigables (`Nueva Escuela Mexicana — DEMO-1`, `Primer grado`) sin exponer IDs técnicos. El perfil pedagógico se edita in-line vía `Section::relationship('profile')` con un `Placeholder` de aviso de privacidad (no incluir nombres, CURP, teléfonos, direcciones ni diagnósticos identificables). Acciones `archive`/`unarchive` sobre `Group`.
- `GroupProfile.revision`: se comporta como contador monótono creciente por ediciones editoriales del docente. Inicializa en 0 al crear el perfil vía `Group::profile()->create()`. Se incrementa `+1` iff `UpdateGroupProfile` detecta que al menos un campo de `PEDAGOGICAL_FIELDS` (todos excepto PK, timestamps, `group_id` y `revision` misma) cambió respecto al valor previo. Guardar sin cambios NO incrementa. Este contador será la base para snapshots de solicitudes en Subfase 2C — aún NO se implementa ningún snapshot.
- Onboarding pedagógico: separado del hito de identidad. El dashboard `/app` muestra: (i) CTA "Completar mi cuenta" si `onboarding_completed_at` es null (Fase 1); (ii) si Fase 1 está completa pero `hasPedagogicalOnboardingComplete()` es false, invita a crear escuela y grupo; (iii) si ambos están completos, muestra estado listo. La bandera pedagógica es derivada y por tanto siempre consistente con el estado real.
- Aislamiento por propietario: (a) Policies deny-by-default por `owner_id`. (b) Filament `getEloquentQuery` filtra por owner. (c) DB-level: FK compuesta `(school_id, owner_id) → schools(id, owner_id)` impide que `groups.owner_id = A` referencie una `schools.id` cuyo owner es B (probado con `QueryException` en `test_escuela_de_otro_owner_es_rechazada_por_composite_fk`). (d) `CreateSchool`/`CreateGroup` fuerzan `owner_id` desde sesión, ignorando el enviado por el navegador.
- Archivado: `groups.archived_at` nullable. Scope `active()` (`whereNull`) excluye archivados por defecto. Tabla del recurso oculta archivados salvo el filtro "Ver archivados". Grupo archivado NO satisface `hasPedagogicalOnboardingComplete()` (probado). Todavía no hay solicitudes, por lo que no se implementaron dependencias con PlanningRequest.
- Tests nuevos: [tests/Feature/SchoolTest.php](tests/Feature/SchoolTest.php) (7 casos), [tests/Feature/GroupTest.php](tests/Feature/GroupTest.php) (8 casos), [tests/Feature/GroupProfileTest.php](tests/Feature/GroupProfileTest.php) (8 casos), [tests/Feature/OnboardingPedagogyTest.php](tests/Feature/OnboardingPedagogyTest.php) (5 casos) y helper compartido [tests/Feature/PedagogyTestCase.php](tests/Feature/PedagogyTestCase.php) con `customer()/admin()/reviewer()`, `seedPublishedCurriculum()` y `seedFullTeacher()`. Cubre: creación válida; CHECK de `school_type`; aislamiento A/B (view/update/delete); revisor sin acceso (Policy + 403 en `/app/schools`); `owner_id` manipulado ignorado; grupo válido con FK compuesta; escuela ajena rechazada por FK compuesta; versión borrador rechazada por trigger; grado de otra versión rechazado por FK compuesta; versión publicada pero no seleccionable rechazada por trigger; archivado excluido del flujo activo; edición de grupo ajeno bloqueada por policy; listado `/app/groups` no muestra grupos ajenos; perfil 1:1 duplicado rechazado por UNIQUE; incremento y no-incremento de `revision`; validaciones de rango (Validator y CHECK DB); acceso cruzado a perfil bloqueado; `isSufficient` correcto; onboarding pedagógico incompleto sin escuela/grupo; completo tras `seedFullTeacher`; archivado revierte a incompleto; cuenta ajena no influye.
- Suite completa post-cambios: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit --testdox` → `OK (90 tests, 259 assertions)` en 22.5 s. Sin regresiones sobre los 62 previos (28 Fase 1 + 15 Subfase 2A.1 + 16 Subfase 2A.2 + 3 protección editorial + 28 Subfase 2B).
- Recursos/páginas Filament creados en el panel `/app`: `SchoolResource` con páginas `ListSchools`/`CreateSchool`/`EditSchool`; `GroupResource` con páginas `ListGroups`/`CreateGroup`/`EditGroup`. Sin resources adicionales en `/admin` o `/review` en 2B (admin conserva acceso vía Policies; revisor sin acceso operativo).
- Diferencia frente a DATABASE.md: no se creó FK real `preferred_format_id → institutional_formats(id)` porque la tabla `institutional_formats` pertenece a la fase posterior de formatos. La columna existe como `unsignedBigInteger nullable` y se documenta explícitamente como integración pendiente. Ningún otro campo o índice difiere del contrato.
- Subfases 2C y 2D **NO iniciadas**. No se creó `PlanningRequest`, NUEVA PLANEACIÓN (rápido/avanzado), `CurriculumSuggestionService`, snapshots textuales, pivotes de solicitud, uploads, planes/pagos, IA/prompts, revisión humana, documentos, renderer DOCX/PDF, correcciones, ni notificaciones comerciales.
- Revisión responsive manual realizada — 2026-09-10, cuenta `docente-2b@example.test` sobre el servidor `php artisan serve` (127.0.0.1:8000), navegador Playwright integrado, viewports 375×812 (móvil) y 1440×900 (desktop). Resultado por pantalla (todas devuelven HTTP 200 y `document.documentElement.scrollWidth === clientWidth`, es decir sin scroll horizontal a nivel de página en ambos viewports):
  - `/app/schools` — móvil: header + botón "Nueva escuela" + búsqueda + tabla con scroll interno de columnas (comportamiento nativo de Filament, aceptable); desktop: layout de tabla completo sin desbordamiento.
  - `/app/schools/create` — móvil: formulario apilado a una columna (Nombre, Tipo `Select native(false)`, Estado, Municipio, Notas) con botones Crear / Crear y crear otro / Cancelar apilables; validación HTML5 visible al enviar vacío. Desktop: dos columnas según `Section::columns(2)`.
  - `/app/groups` — móvil: tabla con acciones Editar/Archivar/Borrar accesibles y scroll interno de columnas; desktop: tabla completa. Filtro "Ver archivados" operativo.
  - `/app/groups/create` — móvil: **defecto real encontrado y corregido** (ver siguiente viñeta). Tras el fix, formulario apilado a una columna con los 3 selects (`Escuela`, `Currículo`, `Grado`) y los textareas del perfil pedagógico legibles; aviso de privacidad (`Placeholder`) visible en la sección pedagógica; selects usan popover no nativo y son táctiles. Desktop: sección "Datos del grupo" a 2 columnas y sección "Perfil pedagógico" a 3 columnas, sin desbordamiento.
  - `/app/groups/{id}/edit` — móvil: los 14 campos del `Section::relationship('profile')` (student_count, general_level, session_minutes, 11 textareas) se apilan uno por línea con textos legibles; botones Guardar cambios / Cancelar tactiles. Desktop: renderizado a 2+3 columnas junto con la sección de datos base.
- Defecto real detectado y corregido durante la revisión: `app/Filament/App/Resources/Groups/GroupResource.php` importaba `Filament\Forms\Components\Section`, `Filament\Forms\Get` y `Filament\Forms\Set`, clases inexistentes en Filament v5 (el editor mostraba HTTP 500: `Class "Filament\Forms\Components\Section" not found` y luego `TypeError: Argument #1 ($get) must be of type Filament\Forms\Get, Filament\Schemas\Components\Utilities\Get given`). Se corrigieron los `use` a `Filament\Schemas\Components\Section`, `Filament\Schemas\Components\Utilities\Get` y `Filament\Schemas\Components\Utilities\Set`. Los tests de feature no exponían el bug porque no renderizan la página `/app/groups/create` — solo instancian el modelo/resource. Post-fix suite: `OK (90 tests, 259 assertions)`.
- Problemas encontrados durante 2B: (a) el `Select::afterStateUpdated` requirió `live()` en Filament 5 para reaccionar a `curriculum_version_id`; (b) el test inicial de `school_type` inválido chocaba con el cast `SchoolType` de Eloquent antes de llegar al CHECK — se resolvió usando `DB::table('schools')->insert(...)` para bypass del cast; (c) el trigger `groups_curriculum_version_guard` requiere que la versión sea la marcada como `selectable_version_id` incluso si está publicada, lo cual bloquea también publicaciones válidas fuera del árbol seleccionado (comportamiento intencional para 2B — cuando cambien las reglas de "elegible para nuevas operaciones" en fases posteriores, el trigger se ajustará junto con `PlanningRequest`).

### Evidencia Subfase 2C — 2026-09-11

Sólo se implementó la **base de dominio de PlanningRequest**: solicitud (borrador/confirmación), pivotes curriculares, snapshot textual inmutable por revisión y bitácora de estados. NUEVA PLANEACIÓN wizard, `CurriculumSuggestionService`, uploads, planes/pagos, IA y revisión humana siguen pendientes (**Subfase 2D NO iniciada**).

- Migración: [database/migrations/2026_09_11_000001_create_planning_requests.php](database/migrations/2026_09_11_000001_create_planning_requests.php) crea `planning_requests`, `request_input_versions`, `request_state_events`, `request_curricular_contents`, `request_pdas`, `request_articulating_axes` y agrega el UNIQUE INDEX auxiliar `pdas_id_version_grade_unique` sobre `pdas(id, curriculum_version_id, grade_id)` para permitir la FK compuesta de `request_pdas`. FKs compuestas: `(group_id, owner_id) → groups(id, owner_id)` (aislamiento por propietario a nivel DB), `(grade_id, curriculum_version_id) → grades` (grado siempre pertenece a la versión), y por cada pivote la mezcla version/grade se bloquea con FK compuesta hacia la tabla curricular. CHECKs: `status IN (...)` con los 15 valores del enum; `ends_on >= starts_on`. Columnas diferidas de DATABASE.md (`subscription_period_id`, `planning_days`, `planning_units`, `commercial_calculation_snapshot`, `entitlement_snapshot`, `format_version_id`) **omitidas explícitamente**: se añadirán con FKs reales cuando existan planes/periodos/formatos en Fase 3+.
- Enum de estados: [app/Enums/PlanningRequestStatus.php](app/Enums/PlanningRequestStatus.php) con los 15 estados de WORKFLOWS.md. Sólo `BORRADOR` y `ESPERANDO_PAGO` tienen transiciones implementadas en 2C; los estados restantes existen para consistencia futura pero ninguna transición los alcanza aún.
- Modelos: [app/Models/PlanningRequest.php](app/Models/PlanningRequest.php) (constante `TRACKED_INPUT_FIELDS` con los 12 campos que disparan bump de `input_revision`; casts, relaciones, scopes `ownedBy`/`drafts`), [app/Models/RequestInputVersion.php](app/Models/RequestInputVersion.php) (append-only: `UPDATED_AT = null` y hooks `booted()` que lanzan `RuntimeException('REQUEST_INPUT_VERSION_IMMUTABLE')` en updating/deleting) y [app/Models/RequestStateEvent.php](app/Models/RequestStateEvent.php) (bitácora append-only). Adicionalmente el trigger PL/pgSQL `request_input_versions_immutability_trg` bloquea UPDATE/DELETE también a nivel PostgreSQL (probado con `DB::table()->update()`), garantizando inmutabilidad incluso si un bug en el modelo intentara escribir.
- Acciones: [app/Actions/Planning/UpdatePlanningRequestDraft.php](app/Actions/Planning/UpdatePlanningRequestDraft.php) valida los 12 campos rastreados, autoriza `update`, exige `BORRADOR`, toma `lockForUpdate` y sólo incrementa `input_revision` si detecta cambio real campo a campo (fechas normalizadas a `Y-m-d`); guardar sin cambios NO incrementa. [app/Actions/Planning/SyncPlanningRequestSelections.php](app/Actions/Planning/SyncPlanningRequestSelections.php) valida en aplicación que cada contenido/PDA/eje pertenezca a la versión del grupo y que cada PDA pertenezca al grado (códigos de error `CONTENT_VERSION_MISMATCH`, `PDA_VERSION_MISMATCH`, `PDA_GRADE_MISMATCH`, `AXIS_VERSION_MISMATCH`), compara los IDs actuales vs los nuevos y sólo hace `sync()` + bump `selection_revision`+`input_revision` si el conjunto cambió; los pivotes escriben `curriculum_version_id` (y `grade_id` para PDAs) para blindar la FK compuesta. [app/Actions/Planning/ConfirmPlanningRequest.php](app/Actions/Planning/ConfirmPlanningRequest.php) autoriza `confirm`, valida `assertReady()` (dates presentes, project o topic presente, `group->profile->isSufficient()`, ≥1 contenido y ≥1 PDA seleccionados y cada contenido con ≥1 PDA seleccionado — códigos `PLANNING_REQUEST_GROUP_PROFILE_INSUFFICIENT`, `PLANNING_REQUEST_NO_CONTENT_SELECTED`, `PLANNING_REQUEST_NO_PDA_SELECTED`, `PLANNING_REQUEST_CONTENT_WITHOUT_PDA:<code>`), construye el JSON snapshot con estructura documentada abajo, crea la fila en `request_input_versions` (revision = input_revision+1) y actualiza `planning_requests` (input_snapshot, current_version_id, curriculum_confirmed_at, status=ESPERANDO_PAGO, lock_version+1). Cierra registrando `RequestStateEvent(BORRADOR → ESPERANDO_PAGO, reason='draft_confirmed')`.
- Estructura del snapshot JSON (`schema_version = 1`): raíz con `confirmed_at`, `confirmed_by`, `input_revision`, `selection_revision`, `request` (12 campos rastreados + `creation_mode`), `group` (id, name, school_year, school anidada, `profile.revision` + todos los campos de `GroupProfile::PEDAGOGICAL_FIELDS`), y `curriculum` con `curriculum{id,code,name}`, `version{id,number,label,checksum,published_at}`, `phase{id,code,name}`, `grade{id,code,name,ordinal}`, `formative_fields[]`, `contents[]` (id, code, title, **full_text**, phase_code, field_code), `pdas[]` (id, code, **full_text**, content_id, grade_id) y `axes[]`. El snapshot es autocontenido: los textos completos quedan congelados aunque después se modifique el catálogo o se retire `selectable_version_id`.
- Semántica de revisiones: `input_revision` avanza por cada edición real (Update o Sync que cambie el conjunto seleccionado); `selection_revision` avanza sólo cuando los IDs de contenidos/PDAs/ejes cambian. En la confirmación, se emite la revisión `input_revision+1` para congelar el estado consistente que el docente vio al presionar "Confirmar". Guardar sin cambios NO incrementa ningún contador (verificado por test).
- Restricciones a nivel PostgreSQL: (a) `groups → planning_requests` por `(group_id, owner_id)` bloquea manualmente escribir una solicitud sobre un grupo ajeno (probado con `QueryException`); (b) `(grade_id, curriculum_version_id) → grades` bloquea usar un grado de otra versión (probado); (c) `(request_id, curriculum_version_id) → planning_requests(id, curriculum_version_id)` en cada pivote bloquea intentos manuales de mezclar versiones (probado insertando directo en `request_curricular_contents`); (d) `request_pdas` valida también grado por FK compuesta `(pda_id, curriculum_version_id, grade_id) → pdas`; (e) el trigger `request_input_versions_immutability_trg` rechaza UPDATE/DELETE a nivel driver.
- Policies: [app/Policies/PlanningRequestPolicy.php](app/Policies/PlanningRequestPolicy.php) — cliente activo verificado ve/edita/confirma/borra sus propias solicitudes; admin lee todas pero **no** puede editar/confirmar borradores ajenos; revisor **sin acceso** en 2C (los estados de revisión aparecerán en Subfase 2D). Edición y confirmación exigen `status = BORRADOR` a nivel policy (defensa en profundidad).
- Filament `/app`: [PlanningRequestResource](app/Filament/App/Resources/PlanningRequests/PlanningRequestResource.php) registrado en `AppPanelProvider`. `getEloquentQuery()` filtra por `owner_id = auth()->id()`. Formulario en 2 secciones: "Datos de la solicitud" (Select de grupo — desactivado en edición, DatePicker fechas, project requerido, textareas topic/book_pages/required_activities/special_events/pedagogical_notes/comments/requested_assessment, period_label) y "Selección curricular" (`Placeholder` de aviso + 3 Selects múltiples de contenidos/PDAs/ejes filtrados por `group.curriculum_version_id`/`grade_id`). Tabla muestra project, grupo, fechas, badge del status en español (`Borrador` gris, `Confirmada · Esperando plan/pago` warning), `input_revision`, `updated_at`. ViewAction siempre visible; EditAction y DeleteAction solo si `isDraft()`. Header action "Confirmar y congelar snapshot" (warning, `requiresConfirmation`, visible sólo si borrador) delega en `ConfirmPlanningRequest` y redirige a la vista.
- Páginas Filament: [ListPlanningRequests](app/Filament/App/Resources/PlanningRequests/Pages/ListPlanningRequests.php), [CreatePlanningRequest](app/Filament/App/Resources/PlanningRequests/Pages/CreatePlanningRequest.php) (fuerza `owner_id`, `curriculum_version_id` y `grade_id` desde el grupo elegido, arranca en `BORRADOR` y delega selección curricular en `SyncPlanningRequestSelections`), [EditPlanningRequest](app/Filament/App/Resources/PlanningRequests/Pages/EditPlanningRequest.php) (`resolveRecord` scoped por owner; elimina del payload cualquier `owner_id`/`group_id`/`curriculum_version_id`/`grade_id`/`status` manipulado; header action "Confirmar y congelar snapshot"), [ViewPlanningRequest](app/Filament/App/Resources/PlanningRequests/Pages/ViewPlanningRequest.php).
- Aislamiento por propietario: (a) Policies deny-by-default; (b) Filament query filtra por owner; (c) FK compuesta `(group_id, owner_id) → groups(id, owner_id)` a nivel DB; (d) `Create*Page` fuerza `owner_id`; (e) `Edit*Page` strippea payload; (f) `resolveRecord` re-filtra por owner. Verificado en tests: Customer A recibe 403/404 en `/app/planning-requests/{id}` y `/edit` de Customer B; `UpdatePlanningRequestDraft` lanza `AuthorizationException`; revisor sin acceso.
- Factory: [PlanningRequestFactory](database/factories/PlanningRequestFactory.php) sin defaults para FKs (owner/group/version/grade) — el caller debe armar la escena válida vía `PedagogyTestCase::seedFullTeacher()`, que ahora expone también `otherGrade`, `draftVersion`, `draftGrade`, `curriculum`.
- Tests nuevos: [tests/Feature/PlanningRequestTest.php](tests/Feature/PlanningRequestTest.php) (8 casos — creación válida, rechazo de grupo/grade ajeno por FK compuesta, edición y no-op sin bump, sync válido con bump de ambas revisiones, PDA/contenido de versión o grado incorrectos rechazados por la acción, pivote manual con versión incorrecta rechazado por FK compuesta), [tests/Feature/PlanningRequestSnapshotTest.php](tests/Feature/PlanningRequestSnapshotTest.php) (8 casos — confirm crea snapshot con textos completos, `profile.revision` y checksum de la versión; snapshot congelado ante cambios posteriores del perfil pedagógico; snapshot congelado ante cambio de `selectable_version_id`; RuntimeException al intentar update Eloquent en `RequestInputVersion`; QueryException al intentar UPDATE por driver; confirm rechazado por perfil insuficiente; confirm rechazado por contenido sin PDA seleccionado — probado con un currículo aislado con 2 contenidos y sólo 1 PDA elegido; confirm rechazado en segundo intento; solicitud confirmada sobrevive el retiro de `selectable_version_id`), [tests/Feature/PlanningRequestSecurityTest.php](tests/Feature/PlanningRequestSecurityTest.php) (4 casos — Customer A no ve ni edita solicitud de B por URL, `UpdatePlanningRequestDraft` lanza `AuthorizationException` para actor ajeno, revisor sin acceso a la URL, manipulación de `owner_id` en payload de edición ignorada).
- Suite completa post-cambios: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit --testdox` → `OK (111 tests, 306 assertions)` en 34.9 s. Sin regresiones sobre los 90 previos: **+21 tests nuevos, +47 aserciones**.
- Revisión responsive manual — 2026-09-11, cuenta `docente-2b@example.test` con `php artisan serve` (127.0.0.1:8000), navegador Playwright integrado, viewports 375×812 (móvil) y 1440×900 (desktop). Todas las pantallas devuelven `document.documentElement.scrollWidth === clientWidth` (sin scroll horizontal global) y no lanzan excepciones de Filament. Cobertura por pantalla:
  - `/app/planning-requests` (listado) — móvil y desktop: tabla con columnas Proyecto/Grupo/Inicio/Fin/Estado/Rev. entrada/Actualizada/Acciones, badge de estado en español (`Borrador` gris y `Confirmada · Esperando plan/pago` warning), acciones Ver/Editar/Borrar en borrador y sólo Ver en confirmada; paginación funcional.
  - `/app/planning-requests/create` — móvil: formulario apilado con Select de grupo (`native(false)`), fechas no nativas, project + textareas + period_label a una columna, sección "Selección curricular" con 3 multiselects (contenidos, PDA, ejes) y aviso de reglas server-side; validación HTML muestra mensajes en español al enviar vacío. Desktop: dos columnas por sección.
  - `/app/planning-requests/{id}/edit` (borrador) — móvil y desktop: mismo layout que create, con Select de grupo **deshabilitado** (defensa contra cambio de grupo tras crear), header action "Confirmar y congelar snapshot" visible, botones Guardar cambios / Cancelar táctiles.
  - `/app/planning-requests/{id}` (vista) — borrador y confirmada: todos los campos deshabilitados, sin action de edición cuando el estado es distinto de BORRADOR; header sin botón "Confirmar" en confirmada.
  - `/app/planning-requests/{id}/edit` sobre solicitud confirmada → HTTP 403 (policy `update` rechaza no-borrador).
- Defecto real detectado y corregido durante la revisión: [app/Filament/App/Resources/PlanningRequests/PlanningRequestResource.php](app/Filament/App/Resources/PlanningRequests/PlanningRequestResource.php) usaba `DatePicker::make('ends_on')->...->after_or_equal('starts_on')`, método inexistente en Filament v5 (`BadMethodCallException: Method Filament\Forms\Components\DatePicker::after_or_equal does not exist`) — se corrigió a `->afterOrEqual('starts_on')`. Los tests de feature no exponían el bug porque no renderizan la página `/create`, sólo instancian el modelo/resource. Post-fix suite: `OK (111 tests, 306 assertions)`.
- Diferencia frente a DATABASE.md: se difieren `subscription_period_id`, `planning_days`, `planning_units`, `commercial_calculation_snapshot`, `entitlement_snapshot` y `format_version_id` con documentación explícita en el archivo de migración (se añadirán con sus FKs reales en Fase 3 cuando existan planes/periodos/formatos). El resto de columnas y restricciones coincide con el contrato.
- **Subfase 2D NO iniciada**: no se implementó NUEVA PLANEACIÓN (wizard rápido/avanzado), `CurriculumSuggestionService`, uploads en cuarentena, planes/pagos, IA, revisión humana, renderer DOCX/PDF, correcciones, notificaciones comerciales ni ningún estado más allá de `BORRADOR → ESPERANDO_PAGO`.

### Evidencia Subfase 2D — 2026-09-12

Se implementó **NUEVA PLANEACIÓN wizard rápido/avanzado** con `CurriculumSuggestionService` determinista y sin IA, reutilizando en su totalidad las acciones de dominio de la Subfase 2C (`UpdatePlanningRequestDraft`, `SyncPlanningRequestSelections`, `ConfirmPlanningRequest`). No se avanzó a Fase 3.

- Servicio: [app/Services/Planning/CurriculumSuggestionService.php](app/Services/Planning/CurriculumSuggestionService.php) — estrategia `deterministic_v1`. Normaliza texto (lowercase, elimina acentos y puntuación), tokeniza con lista de stopwords en español y descarta tokens < 3 caracteres. Contenidos: filtrados por versión + `whereHas('pdas', fn ($q) => $q->where('grade_id', $gradeId))` + puntaje ponderado (título ×3, full_text ×2); top-N con `arsort` y `orderBy id` para desempate determinista. PDAs: dentro de versión + grado + top contenidos, puntaje baseline 1 + 1 por token en full_text; top-N por contenido. Ejes: match sobre `name` + `description`. `formative_field_ids` derivados de los contenidos top. `has_strong_match = max(score) >= 3`. Vacío en tokens ⇒ resultado vacío. **Sin llamadas externas ni IA.**
- Acción wrapper: [app/Actions/Planning/GenerateCurriculumSuggestions.php](app/Actions/Planning/GenerateCurriculumSuggestions.php) — autoriza `update` sobre la solicitud vía `Gate::forUser`, rechaza no-borrador con `RuntimeException('PLANNING_REQUEST_ALREADY_CONFIRMED')` y compone la entrada al servicio a partir de los campos ya escritos por el docente + overrides opcionales del wizard.
- Wizard Filament: [app/Filament/App/Resources/PlanningRequests/PlanningRequestResource.php](app/Filament/App/Resources/PlanningRequests/PlanningRequestResource.php) reescrito con `Filament\Schemas\Components\Wizard` de 4 pasos: (1) **Grupo y modalidad** — Select `group_id` (`disabledOn('edit')`) filtrado por `eligibleGroupOptions()` (owner + no archivado + `profile->isSufficient()` + versión publicada) y Radio `creation_mode` `quick|advanced` (default `quick`); (2) **Datos básicos** — fechas, project (requerido, min 3), textareas de topic/book_pages/required_activities/special_events/comments, period_label, hint de duración; (3) **Selección curricular** — botón **"Sugerir alineación curricular"** (visible sólo en modo `quick`, con tooltip "Búsqueda determinista por palabras clave sobre el catálogo curricular publicado. No usa IA ni servicios externos.") que llama el servicio y **fusiona sin sobrescribir** la selección existente + tres multiselects `contents/pdas/axes` con `native(false)`, `preload` y `->live()` para refiltrar PDAs por contenidos seleccionados; el cambio de contenidos poda automáticamente PDAs huérfanos (evita `validation.in`); (4) **Resumen** — `dl` con Grupo/Grado/Currículo/Modalidad/Fechas/Tema/Contenidos/PDA/Ejes (labels, no IDs). Wizard `persistStepInQueryString('paso')` y `skippable` en edición. Tabla del listado añadió columna badge `creation_mode` (`Rápido`/`Avanzado`/`—`).
- Reutilización de acciones existentes: el wizard **no crea nuevas acciones de escritura**. `CreatePlanningRequest` fuerza `owner_id`, `curriculum_version_id`, `grade_id` desde el grupo, deja pasar `creation_mode` por fillable y delega la selección curricular en `SyncPlanningRequestSelections`. `EditPlanningRequest` mantiene el header action, ahora rotulado **"Confirmar planeación"** con notificación `Tu solicitud quedó guardada. El siguiente paso será activar el procesamiento.`, delegando en `ConfirmPlanningRequest`. `UpdatePlanningRequestDraft` incorporó `creation_mode` como campo rastreado (`TRACKED_INPUT_FIELDS` + validación `in:quick,advanced`) para que un cambio de modalidad también incremente `input_revision`.
- CTA principal: [resources/views/filament/app/pages/dashboard.blade.php](resources/views/filament/app/pages/dashboard.blade.php) muestra el botón **"Nueva planeación"** (`heroicon-o-document-plus`, tamaño `lg`) apuntando a `PlanningRequestResource::getUrl('create')` cuando el usuario terminó onboarding y perfil pedagógico.
- Tests nuevos: [tests/Feature/CurriculumSuggestionServiceTest.php](tests/Feature/CurriculumSuggestionServiceTest.php) (10 casos — devuelve sólo IDs del catálogo; filtra por versión y grado; título prevalece sobre coincidencia únicamente en PDA; encuentra por full_text cuando el título no coincide; nunca devuelve PDAs de otro grado; nunca devuelve items de otra versión aunque coincidan textualmente; sin coincidencias ⇒ vacío + `has_strong_match=false`; determinismo entrada-idéntica ⇒ salida-idéntica; ejes sólo cuando `name`/`description` coinciden; ignora stopwords y tokens < 3 letras) y [tests/Feature/NewPlanningWizardTest.php](tests/Feature/NewPlanningWizardTest.php) (8 casos — Dashboard muestra CTA; `/app/planning-requests/create` responde 200 con los 4 pasos; `eligibleGroupOptions()` esconde grupos con perfil incompleto; opciones de contenido/PDA respetan versión+grado; wizard rápido→avanzado no borra selección ni datos y `input_revision` avanza vía `UpdatePlanningRequestDraft`; flujo completo confirma vía `ConfirmPlanningRequest` y llega a `ESPERANDO_PAGO` con `creation_mode` en snapshot; opciones de PDA se acotan al filtrar por contenido; helpers de opciones ignoran completamente grupos de otro dueño).
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` → `OK (129 tests, 351 assertions)` en 48.3 s. **+18 tests / +45 aserciones** sobre el baseline 2C.
- Diferencias vs MVP declarado: no se añadió tabla `curriculum_suggestions` cache (POST-MVP explícito en CURRICULUM.md — la estrategia es determinista y se recalcula en el momento); no se tocaron `subscription_period_id`, `format_version_id` ni columnas comerciales (siguen diferidas para Fase 3); **no se implementó IA** — el botón se rotula "Sugerir alineación curricular" con tooltip explícito "No usa IA ni servicios externos"; internamente ejecuta la estrategia local `deterministic_v1`.
- Revisión responsive manual — 2026-09-12, cuenta `docente-2b@example.test` (con `onboarding_completed_at` completado en tinker antes de la auditoría), `php artisan serve` (127.0.0.1:8000), viewports 375×812 y 1440×900, navegador Playwright integrado. `document.documentElement.scrollWidth === clientWidth` verificado programáticamente para `/app`, `/app/planning-requests`, `/app/planning-requests/create` y `/app/planning-requests/2` (vista de una confirmada) en ambos viewports; ninguna pantalla disparó `dialog#livewire-error`. Flujo completo del wizard recorrido en 375×812 con `docente-2b@example.test` y grupo `1° A`: (a) Paso 1 grupo `1° A` + modo Rápido; (b) Paso 2 fechas 2026-10-01/2026-10-31 (hint muestra `31 día(s)`), project + topic; (c) Paso 3 botón "Sugerir alineación curricular" con datos que no coinciden en el catálogo demo dispara la notificación en español "No encontramos una coincidencia clara"; con project "Contenido demo lenguajes" retorna 2 contenidos, 2 PDA y 2 ejes coherentes; al quitar uno de los contenidos, los PDA huérfanos se podan automáticamente y el paso avanza sin `validation.in`. Verificado que `/app/planning-requests/2/edit` sobre una solicitud confirmada devuelve **HTTP 403** (policy).
- Ajustes de UX/copy detectados y corregidos en la auditoría: (i) el botón de sugerencias salía como "Obtener sugerencias" — se cambió a "Sugerir alineación curricular" y se añadió el tooltip "Búsqueda determinista por palabras clave sobre el catálogo curricular publicado. No usa IA ni servicios externos." para eliminar cualquier lectura de IA; (ii) las Placeholders `duration_hint`, `curriculum_intro`, `summary` y `confirm_hint` mostraban el nombre interno ("Duration hint", "Curriculum intro") como `<dt>` porque `->label('')` no lo suprime en Filament 5 — se sustituyó por `->hiddenLabel()` (o `->label('Duración')` en el caso del hint de fechas) para que el DOM no exponga nombres técnicos; (iii) al deseleccionar un contenido después de aceptar sugerencias, el multiselect de PDA quedaba con valores huérfanos y el wizard mostraba `validation.in` al pulsar Siguiente — se agregó `afterStateUpdated` en `selected_contents` que poda automáticamente las PDA cuyo contenido ya no está en la selección. Ninguno afectó al servicio, acciones de dominio ni tests existentes; suite completa reejecutada `OK (129 tests, 351 assertions)` en 30.9 s.
- **Fase 3 NO iniciada**: no se implementó `PlanVersion`, cotización `U = ceil(D/M)`, periodos, reservas, IA, uploads, revisión humana, correcciones ni renderer DOCX/PDF.

## Fase 3 — Planes y pagos

- [x] **Subfase 3A — Planes y reglas comerciales**: `Plan` + `PlanVersion` versionada e inmutable, `PlanningUnitCalculator` con estrategia `calendar_days_v1` (`U = ceil(D/M)`, días naturales inclusivos), límites `max_planning_days` / `planning_limit` / `human_review_limit` / `correction_limit` / `group_limit`, CRUD mínimo en `/admin`, seeder de planes DEMO. Ver evidencia bajo "Evidencia Subfase 3A".
- [x] **Subfase 3B — Suscripciones, periodos y unidades**: `Subscription` (customer/plan/plan_version exactos, estados `pending/active/past_due/cancelled/expired`, índice único parcial `active|past_due` por cliente), `SubscriptionPeriod` con `entitlement_snapshot` congelado, ventana `[starts_at, ends_at)` sin solape (EXCLUDE gist), `UsageReservation` con `operation_key` UNIQUE, transiciones `reserved→consumed|released` estrictas y balance `available = limit − reserved − consumed`. Corrección por ronda/solicitud (`client_correction` como resource) y límites de grupos **quedan como columnas del snapshot** pero su enganche con `PlanningRequest` y correcciones se difiere a 3D. Ver evidencia bajo "Evidencia Subfase 3B".
- [ ] Probar D=M, M+1, varios M, mes/año, sin saldo U, cambio de plan/fechas y reserva concurrente. *(Parcialmente cubierto en 3B con tests de idempotencia, insuficiencia, ventana y liberación; pruebas de cambio de plan/fechas se retoman en 3C/3D con integración de PlanningRequest.)*
- [x] **Subfase 3C — Órdenes, pagos y activación comercial**: `Order` (estados `pending/paid/cancelled/refunded`), `Payment` (`pending/succeeded/failed/cancelled`), `PaymentEvent` (dedupe por `(provider, event_id)` UNIQUE), `Refund` (`pending/succeeded/failed/cancelled`), contrato `PaymentGateway` + adapter `ManualPaymentGateway`, Actions `CreateOrder`/`CancelOrder`/`RecordManualPayment`/`ActivatePaidOrder`/`RecordManualRefund`. Ver evidencia bajo "Evidencia Subfase 3C".
- [x] **Subfase 3D — Autorización comercial de PlanningRequest**: `AuthorizePlanningRequestForProcessing` reserva unidades atómicamente contra el `SubscriptionPeriod` vigente, congela `PlanVersion` histórica + `PlanningRequestSegment` + `calculation_snapshot`, transiciona `ESPERANDO_PAGO → LISTA_PARA_PROCESAR` de forma idempotente, valida `group_limit` server-side y expone la página `/app/mi-plan` con saldo en tiempo real. Ver evidencia bajo "Evidencia Subfase 3D".
- [ ] Renovación/cancelación de periodo; UI de plan, uso y pagos propios.
- [ ] Probar concurrencia, doble confirmación, cuota agotada, devolución parcial y expiración.

Salida: ningún doble cargo lógico ni sobreconsumo; no confundir pago manual con gateway real.

### Evidencia Subfase 3A

- Migración `2026_09_12_000001_create_plans_and_plan_versions.php`: tablas `plans` (identidad estable con `code UNIQUE`) y `plan_versions` (fotografía comercial con `plan_id + number` UNIQUE). CHECKs a nivel Postgres: `max_planning_days>0`, `planning_limit>0`, `group_limit>0`, `interval_count>0`, `human_review_limit>=0`, `correction_limit>=0`, `price_minor>=0`, `interval_unit IN ('month','year')`, rango `effective_from <= effective_until`, coherencia `human_review_required=false OR human_review_limit>0`. Inmutabilidad garantizada por trigger PL/pgSQL `plan_version_immutability_guard` que rechaza UPDATE/DELETE cuando `OLD.published_at IS NOT NULL` y exige `published_by` + `checksum` en la transición de publicación. Se difieren explícitamente `subscription`, `subscription_period`, `usage_reservation`, `order`, `payment`, `payment_gateway`, webhooks, checkout, saldo del usuario, activación tras pago, consumo real y enganche comercial con `PlanningRequest` (todo para 3B/3C/3D/Fase 4).
- Modelos `App\Models\Plan` (fillable code/name/description/active) y `App\Models\PlanVersion` (casts `features:array`, `human_review_required:bool`, `published_at:datetime`) con hooks Eloquent `updating`/`deleting` que lanzan `PLAN_VERSION_PUBLISHED_IMMUTABLE` para fallar temprano antes del trigger BD. Factories `PlanFactory` y `PlanVersionFactory` con estados `reviewed()` y `published()`.
- Servicio `App\Services\Planning\PlanningUnitCalculator` con constante `STRATEGY = 'calendar_days_v1'`. Fórmula: `D = start->diffInDays(end) + 1` sobre `startOfDay()` (naturales inclusivos, sin excluir fines de semana ni festivos), `U = ceil(D/M)`. Devuelve además `segments[]` consecutivos de hasta M días sin solape ni hueco. Rechaza `PLANNING_DATE_RANGE_INVALID` y `PLAN_VERSION_INVALID_MAX_PLANNING_DAYS`.
- Acción `App\Actions\Plans\PublishPlanVersion` (transacción con `lockForUpdate` de plan + versión) que valida coherencia comercial, calcula `checksum = sha256(payload_ordenado)` y setea `published_at`, `published_by`, `checksum`. Códigos semánticos: `PLAN_VERSION_ALREADY_PUBLISHED`, `PLAN_VERSION_HUMAN_REVIEW_LIMIT_REQUIRED`, `PLAN_VERSION_INVALID_*`.
- Políticas `PlanPolicy` y `PlanVersionPolicy` (traits `AuthorizesCurriculumTree`): sólo administrador activo + email verificado puede crear/publicar; docente-cliente y docente-revisor pueden ver el catálogo; borrar sólo si está en borrador; delete de `Plan` bloqueado si tiene versiones publicadas.
- Recursos Filament `/admin`: `PlanResource` (list/create/edit) y `PlanVersionResource` (list/create/edit) con acciones `EditAction`/`DeleteAction` visibles sólo para versiones en borrador y acción `publish` que invoca la Action de dominio. Registrados en `AdminPanelProvider`.
- Seeder idempotente `Database\Seeders\DemoPlansSeeder` que crea y publica **BASIC-DEMO** (M=7, planning_limit=4, human_review_limit=0, correction_limit=1, group_limit=1, human_review_required=false) y **REVIEWED-DEMO** (M=7, planning_limit=4, human_review_limit=4, correction_limit=2, group_limit=2, human_review_required=true). Precio 0 y moneda MXN son placeholders sin validez comercial hasta 3B.
- Tests añadidos (24 assertions nuevas, 12 tests nuevos):
  - `PlanningUnitCalculatorTest`: dataProvider con M=7 para 1→1, 7→1, 8→2, 14→2, 15→3, 28→4, 31→5, fin de semana inclusivo; segmentación de 18 días → 3 segmentos `[1-7, 8-14, 15-18]` sin solape ni hueco; rechazo de rango invertido y `M<=0`.
  - `PlanVersionTest`: publicar como admin, `PLAN_VERSION_ALREADY_PUBLISHED` en doble publicación, CHECK BD de coherencia revisión humana, unicidad `(plan_id,number)`, valores inválidos rechazados por CHECK, publicada inmutable vía Eloquent (`PLAN_VERSION_PUBLISHED_IMMUTABLE`) y vía trigger BD (`QueryException` con `PLAN_VERSION_PUBLISHED`), nueva versión draft coexiste con publicada, autorizaciones de customer/reviewer/admin.
  - `PlanTest`: admin puede crear, customer/reviewer no; código único protegido por índice.
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` ⇒ `OK (153 tests, 413 assertions)` en 37.3 s (24 tests nuevos vs los 129 previos).
- Rutas Filament confirmadas por `artisan route:list --path=admin`: `admin/plans`, `admin/plans/create`, `admin/plans/{record}/edit`, `admin/plan-versions`, `admin/plan-versions/create`, `admin/plan-versions/{record}/edit`.
- **Auditoría responsive completada 2026-09-08** con `php artisan serve` (127.0.0.1:8000), cuenta `admin@example.test`, datos DEMO del `DemoPlansSeeder`, navegador Playwright integrado. `document.documentElement.scrollWidth === clientWidth` verificado programáticamente en 375×812 y 1440×900 para las 6 rutas: `/admin/plans` (overflow=0/0), `/admin/plans/create` (0/0), `/admin/plans/{id}/edit` (0/0), `/admin/plan-versions` (0/0), `/admin/plan-versions/create` (0/0) y `/admin/plan-versions/{id}/edit` (0/0 en drafts; 403 correcto en publicadas). Ningún runtime error de Filament, ninguna consola marca `livewire-error`. Tablas usables, formularios apilados en móvil, botones táctiles (>=40px de altura Filament por defecto).
- **Comprobación funcional realizada**: (i) crear Plan DEMO desde `/admin/plans/create` ✓; (ii) crear PlanVersion borrador `BASIC-DEMO v2` (M=10, planning_limit=6, price=15000 centavos) ✓; (iii) intento de crear con `max_planning_days=0` rechazado por HTML5 `min=1` + CHECK BD `plan_versions_max_planning_days_positive` (cubierto por `PlanVersionTest::rechaza_valores_invalidos_a_nivel_bd`) ✓; (iv) publicar la borrador desde la tabla mediante `Action::make('publish')` → notificación "Versión publicada" + estado pasa a "Publicada" ✓; (v) `/admin/plan-versions/3/edit` (publicada) devuelve **HTTP 403** y las acciones `Editar`/`Publicar`/`Borrar` desaparecen de la fila ✓; (vi) `BASIC-DEMO v1` histórica permanece intacta (misma `published_at` y checksum) tras publicar `v2` ✓.
- **Defectos encontrados y corregidos dentro de 3A**:
  - `PlanVersionResource::table()` tenía dos columnas con la misma clave `published_at` (una `IconColumn` "Publicada" y una `TextColumn` "Publicación"); Filament fusionaba ambas y la columna de icono no aparecía. Se sustituyó por una `TextColumn::make('status')` badge (`Publicada`=verde / `Borrador`=ámbar) generada vía `getStateUsing`, y la `TextColumn::make('published_at')` de fecha usa `placeholder('—')` para borradores.
  - Formulario de `PlanVersionResource` mostraba etiquetas técnicas crudas (`max_planning_days (M)`, `planning_limit`, `human_review_limit`, `correction_limit`, `group_limit`, `correction_window_days`, `sla_hours`, `Intervalo`, `Precio (centavos)`, `Moneda ISO 4217`); se renombraron a español legible + `helperText` con la definición operativa (ej. "M en la fórmula U = ceil(D / M)", "0 si el plan no incluye revisión humana", "No se multiplica por U", "Ej. 12345 = 123.45 en la moneda indicada"). Opciones de `interval_unit` mostradas como `Mes` / `Año`. No se tocaron los nombres de columna en BD ni la Action.
- **Refactor del trait de autorización**: `AuthorizesCurriculumTree` sólo contenía `isAdmin()` (activo + verificado + rol Administrator) y `canReadCatalog()` (cualquier rol activo verificado). Cero lógica curricular. Se renombró a `AuthorizesActiveRoleUser` (mismo archivo movido con `git mv`) y se actualizaron los 5 policies consumidores (`CurriculumPolicy`, `CurriculumVersionPolicy`, `CurriculumElementPolicy`, `PlanPolicy`, `PlanVersionPolicy`) para que el nombre refleje la responsabilidad real. Suite completa reejecutada `OK (153 tests, 413 assertions)` en 52.3 s; ninguna regresión.
- **Subfase 3B, 3C, 3D y Fase 4 NO iniciadas**: no se implementó `Subscription`, `SubscriptionPeriod`, `UsageReservation`, `Order`, `Payment`, `PaymentGateway`, webhook, checkout, saldo del usuario, activación tras pago, consumo real de unidades, enganche comercial con `PlanningRequest`, IA, generación, revisión humana, documentos, renderer DOCX/PDF, notificaciones comerciales, máquina de estados de pipeline ni outbox.

### Evidencia Subfase 3B

- Migración `2026_09_13_000001_create_subscriptions_and_usage_ledger.php` con tres tablas nuevas y toda la ingeniería de invariantes en Postgres:
  - `subscriptions`: `customer_id`, `plan_id`, `plan_version_id`, `status`, `starts_at`, `renews_at`, `cancel_requested_at`, `ends_at`. CHECK `status IN ('pending','active','past_due','cancelled','expired')`. **Índice único parcial** `subscriptions_customer_operational_uniq ON (customer_id) WHERE status IN ('active','past_due')` garantiza "una suscripción vigente por cliente" a nivel BD. FK compuesta `(plan_version_id, plan_id) → plan_versions(id, plan_id)` cierra la coherencia customer/plan/plan_version verbatim. Trigger PL/pgSQL `subscription_plan_version_published_guard` rechaza suscripciones apuntando a versiones no publicadas (código `SUBSCRIPTION_PLAN_VERSION_NOT_PUBLISHED`).
  - `subscription_periods`: `subscription_id`, `plan_id`, `plan_version_id`, `starts_at`, `ends_at`, `status`, `entitlement_snapshot JSONB`, `paid_order_id NULLABLE`. Ventana `[starts_at, ends_at)` con CHECK `ends_at > starts_at`. **`EXCLUDE USING gist` con `tstzrange(starts_at, ends_at, '[)')`** (extensión `btree_gist`) prohíbe solapes entre periodos `pending|active` de la misma subscription. UNIQUE `(subscription_id, starts_at)`. FK compuesta hacia `subscriptions(id, plan_id)` y `plan_versions(id, plan_id)` propaga la coherencia. `paid_order_id` queda sin FK ("deferido a 3C" documentado en migración).
  - `usage_reservations`: `subscription_period_id`, `planning_request_id NULLABLE` (FK real; sin wiring automático desde `PlanningRequest`, tal como pide el brief), `correction_request_id NULLABLE` sin FK (tabla `correction_requests` inexistente en 3B), `resource`, `operation_key VARCHAR UNIQUE`, `quantity UnsignedInt CHECK >0`, `status`, `reserved_at`, `consumed_at NULLABLE`, `released_at NULLABLE`. CHECKs semánticos: `resource IN (planning|human_review|client_correction)`, `status IN (reserved|consumed|released)`, `(status='consumed') = (consumed_at IS NOT NULL)`, `(status='released') = (released_at IS NOT NULL)`. Trigger PL/pgSQL `usage_reservation_transition_guard` bloquea (a) DELETE de reservas ya `consumed|released`, (b) mutación de `subscription_period_id|resource|quantity|operation_key`, (c) transición fuera de `reserved`, (d) transición a estados no `consumed|released`. Códigos: `USAGE_RESERVATION_TERMINAL_STATE`, `USAGE_RESERVATION_INVALID_TRANSITION`, `USAGE_RESERVATION_PERIOD_LOCKED`, `USAGE_RESERVATION_RESOURCE_LOCKED`, `USAGE_RESERVATION_QUANTITY_LOCKED`, `USAGE_RESERVATION_OPERATION_KEY_LOCKED`.
- Enums `App\Enums\SubscriptionStatus`, `SubscriptionPeriodStatus`, `UsageResource`, `UsageReservationStatus`. `SubscriptionStatus::isOperational()` retorna true para `active|past_due` y alimenta el índice único parcial; `UsageResource::consumesPeriodEntitlement()` distingue `planning|human_review` (consumen contra el snapshot del periodo) del `client_correction` (contabiliza por PlanningRequest — enganche real en 3D).
- Modelos `Subscription`, `SubscriptionPeriod`, `UsageReservation` con casts de enum, `entitlement_snapshot => array`, timestamps `datetime` y relaciones `belongsTo`/`hasMany` completas. `SubscriptionPeriod::isReservable()` requiere `status=Active` y ventana temporal vigente; `SubscriptionPeriod::entitlement($key)` expone accesor tipado sobre el snapshot congelado.
- Value object `App\Services\Commerce\ResourceBalance` (readonly, `limit`, `reserved`, `consumed`, `available()`), servicio `App\Services\Commerce\SubscriptionBalance::forPeriod($period)` que agrega `usage_reservations` por `resource` y `status` y devuelve `{planning: ResourceBalance, human_review: ResourceBalance}`. Fórmula oficial `available = limit − reserved − consumed` verbatim DATABASE.md; `consumed` mantiene ocupación permanente.
- Actions de dominio en `App\Actions\Commerce\`:
  - `CreateSubscription($customer, $planVersion, $startsAt)`: `DB::transaction` + `lockForUpdate` sobre `users` y `plan_versions`; valida `PlanVersion.published_at !== null` (`SUBSCRIPTION_PLAN_VERSION_NOT_PUBLISHED`) y ausencia de suscripción operativa (`SUBSCRIPTION_ALREADY_OPERATIONAL`). Deja la nueva subscription en `active`. Alta comercial post-checkout `pending` se difiere a 3C.
  - `OpenSubscriptionPeriod($sub, $startsAt, $endsAt)`: `lockForUpdate` de la subscription; congela `entitlement_snapshot` con `plan_version_id`, `plan_version_number`, `plan_version_checksum`, `max_planning_days`, `planning_limit`, `human_review_limit`, `correction_limit`, `group_limit`, `correction_window_days`, `sla_hours`, `human_review_required`. Status derivado (`active` si `starts ≤ now < ends`, `pending` en otro caso). Rechaza rango invertido (`SUBSCRIPTION_PERIOD_INVALID_RANGE`) y suscripciones no operativas.
  - `ReservePlanningUnits($period, $resource, $units, $operationKey, $planningRequestId=null)`: **idempotencia** — si existe reserva con misma `operation_key` y payload compatible (mismo periodo/recurso/cantidad) retorna la existente; payload distinto → `USAGE_IDEMPOTENCY_CONFLICT`. **Concurrencia** — `lockForUpdate` sobre `SubscriptionPeriod` serializa el cálculo del balance con otras reservas en vuelo. Rechaza periodos no reservables (`SUBSCRIPTION_PERIOD_NOT_ACTIVE`), unidades ≤0 (`USAGE_RESERVATION_INVALID_UNITS`), planes sin cuota de revisión humana cuando el recurso es `human_review` (`HUMAN_REVIEW_NOT_INCLUDED`) y saldo insuficiente (`INSUFFICIENT_PLANNING_UNITS`).
  - `ConsumePlanningReservation($r)`: `lockForUpdate`, idempotente si ya `consumed`, rechaza `released` con `USAGE_RESERVATION_ALREADY_RELEASED`. No modifica `available` (consumed sigue restando en la fórmula).
  - `ReleasePlanningReservation($r)`: `lockForUpdate`, idempotente si ya `released`, rechaza `consumed` con `USAGE_RESERVATION_ALREADY_CONSUMED`. Devuelve las unidades al `available`.
- Políticas `SubscriptionPolicy`, `SubscriptionPeriodPolicy`, `UsageReservationPolicy` (trait `AuthorizesActiveRoleUser`): admin ve/crea/edita suscripciones; cliente sólo ve las propias; reviewer sin acceso. Periodos y reservas: sin update/delete Filament (mutación exclusiva vía Actions de dominio).
- Recursos Filament en `/admin`:
  - `SubscriptionResource` (list/create/edit): formulario con `Cliente`, `Plan` (activos), `Versión de plan publicada` (`->live()` filtra sólo `published_at IS NOT NULL` del plan seleccionado y muestra `v{n} · publicada YYYY-MM-DD`), `Inicio de la suscripción`, `Estado` disabled + `dehydrated(false)`. La página `CreateSubscription` override `handleRecordCreation` para invocar `CreateSubscription` Action (respeta locks, validaciones y triggers BD). Tabla: badge de estado (`Active`=verde, `PastDue`=amarillo, `Pending`=gris, `Cancelled|Expired`=rojo), contador de periodos.
  - `SubscriptionPeriodResource` (list/view, `canCreate=false`): columnas `Cliente`, `Plan`, `Versión`, `Estado` (badge), `Inicio`, `Fin`, y dos columnas calculadas `Planeaciones (disp/total)` y `Rev. humana (disp/total)` que consumen `SubscriptionBalance::forPeriod()` en vivo.
  - `UsageReservationResource` (list-only, `canCreate=false`): columnas `Cliente`, `Periodo`, `Recurso` (badge en español: Planeación/Revisión humana/Corrección), `Unidades`, `Estado` (badge), `Clave op.` con tooltip completo, `Reservada`, `Consumida`, `Liberada`.
  - Registro en `AdminPanelProvider`.
- Tests añadidos (22 tests, 53 assertions netas nuevas):
  - `tests/Feature/Commerce/SubscriptionLifecycleTest` (8): alta admin ok, rechaza plan_version borrador, índice único parcial rechaza segunda operativa (`SUBSCRIPTION_ALREADY_OPERATIONAL`), cancelación libera slot, snapshot congelado copia todos los límites, publicar nueva versión no muta snapshot histórico, EXCLUDE constraint rechaza solape (`QueryException`), rango invertido rechazado.
  - `tests/Feature/Commerce/UsageLedgerTest` (12): balance inicial completo; reservar reduce `available`; consumir mantiene `available` reducido (consumed cuenta); liberar devuelve unidades; exceder límite → `INSUFFICIENT_PLANNING_UNITS`; reserva parcial + exceso remanente rechazada; idempotencia con mismo `operation_key` + mismo payload; conflicto de idempotencia con payload distinto; consumir idempotente + release-after-consume prohibido; liberar idempotente + consume-after-release prohibido; `HUMAN_REVIEW_NOT_INCLUDED` cuando el plan tiene `human_review_limit=0`; reserva de revisión humana con plan reviewed; rechazo de reserva sobre periodo `ended`.
  - `tests/Feature/Commerce/UsageLedgerConcurrencyGuardsTest` (2): la UNIQUE `operation_key` rechaza inserción cruda que salte la Action; el trigger de inmutabilidad rechaza `UPDATE consumed→released` a nivel BD. Nota metodológica: PHPUnit `RefreshDatabase` envuelve el test en transacción y **no permite** dos conexiones Postgres visibles entre sí; la concurrencia real se apoya en `SELECT FOR UPDATE` dentro de las Actions y en los guardrails de esquema aquí cubiertos.
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` ⇒ `OK (175 tests, 466 assertions)` en ~37 s (22 tests nuevos vs los 153 previos). Ninguna regresión.
- Auditoría responsive completada 2026-09-08 con `php artisan serve --port=8080` y cuenta `admin@example.test`, seed `DemoPlansSeeder` + seed manual `tools/seed_3b_demo.php` (`Docente Demo 3B` con subscription+period+reserva `Planeación quantity=2`). Programáticamente `document.body.scrollWidth > window.innerWidth` = `false` en 375×812 y 1440×900 para las 5 rutas de 3B: `/admin/subscriptions` (status 200/false), `/admin/subscriptions/create` (200/false), `/admin/subscription-periods` (200/false), `/admin/subscription-periods/1` (200/false) y `/admin/usage-reservations` (200/false). Sidebar colapsable en 375, sin overflow horizontal, badges legibles, tabla con `Cliente`, `Recurso=Planeación`, `Unidades=2`, `Estado=Reserved`, `Clave op.=demo-op-1`.
- **Subfase 3C, 3D y Fase 4 NO iniciadas**: no se implementó `Order`, `Payment`, `PaymentGateway`, checkout, tarjeta, Mercado Pago/Stripe, webhook de pago, conciliación, activación automática por pago, integración de reserva con `PlanningRequest`, cambio de estado de `PlanningRequest`, IA, generación, revisión humana operacional, `ReviewerAssignment`, documentos, renderer DOCX/PDF ni notificaciones comerciales. `client_correction` como valor del enum `UsageResource` está aceptado por el ledger pero **no se enlaza a `PlanningRequest`** hasta 3D.

### Evidencia Subfase 3C

- Migración `2026_09_14_000001_create_orders_payments_events_refunds.php` con cuatro tablas nuevas y triggers de dominio:
  - `orders`: `customer_id`, `plan_id`, `plan_version_id` (FK compuesta `(plan_version_id, plan_id) → plan_versions(id, plan_id)` para coherencia customer/plan/plan_version), `subscription_period_id NULLABLE`, `activated_subscription_id NULLABLE` (idempotency link de activación), `total_minor`, `currency`, `status CHECK IN (pending|paid|cancelled|refunded)`, `idempotency_key VARCHAR UNIQUE`, `paid_at NULLABLE`. CHECK `orders_paid_at_consistency` = `(status IN (paid,refunded)) ⇔ (paid_at IS NOT NULL)`. Trigger PL/pgSQL `order_plan_version_published_guard` rechaza órdenes apuntando a versiones no publicadas (`ORDER_PLAN_VERSION_NOT_PUBLISHED`). Trigger `order_paid_immutability_guard` a nivel BD bloquea (a) DELETE de orders `paid|refunded`, (b) mutación de `total_minor|currency|plan_version_id|plan_id|customer_id|idempotency_key` en estados `paid|refunded`, (c) transiciones `paid→pending|cancelled`, (d) mutación de estados terminales `cancelled|refunded`. FK diferida `subscription_periods.paid_order_id → orders.id ON DELETE SET NULL` se agrega en esta migración.
  - `payments`: `order_id`, `customer_id`, `provider VARCHAR(64)`, `provider_reference VARCHAR(255) NULLABLE`, `method`, `amount_minor CHECK >=0`, `currency CHAR(3)`, `status CHECK IN (pending|succeeded|failed|cancelled)`, `occurred_at`, `confirmed_by NULLABLE`. UNIQUE `(provider, provider_reference)` para dedupe de webhooks / referencias bancarias. CHECK `succeeded ⇒ provider_reference IS NOT NULL`. **Índice único parcial** `payments_order_succeeded_uniq ON (order_id) WHERE status='succeeded'` garantiza "un solo pago exitoso por order" a nivel BD. Trigger `payment_transition_guard` bloquea DELETE de `succeeded`, mutación de `succeeded` y salida de estados terminales `failed|cancelled`.
  - `payment_events`: `provider`, `event_id`, `payload_hash CHAR(64)`, `status CHECK IN (received|processed|ignored|failed)`, `received_at`, `processed_at NULLABLE`, `error_code NULLABLE`, `payment_id NULLABLE`, `order_id NULLABLE`. UNIQUE `(provider, event_id)` dedupe webhook idempotente. CHECK `processed_at_consistency` = `(status='processed') ⇔ (processed_at IS NOT NULL)`.
  - `refunds`: `payment_id`, `amount_minor CHECK >0`, `status CHECK IN (pending|succeeded|failed|cancelled)`, `reason`, `provider_reference NULLABLE`, `idempotency_key VARCHAR UNIQUE`, `completed_at NULLABLE`, `confirmed_by NULLABLE`. CHECK `succeeded ⇔ completed_at IS NOT NULL`. Suma refundada ≤ payment enforcada por la Action con `SELECT FOR UPDATE + agregación` (CHECK no puede referenciar múltiples filas).
- Enums `App\Enums\OrderStatus` (con `isTerminal()` para `cancelled|refunded`), `PaymentStatus`, `PaymentEventStatus`, `RefundStatus`. Modelos `Order`, `Payment`, `PaymentEvent`, `Refund` con casts de enum, relaciones completas y helpers `isPaid()`/`isSucceeded()`/`successfulPayment()`.
- Contrato `App\Services\Commerce\Gateway\PaymentGateway` (`code(): string` + `recordManualPayment(Order, ManualPaymentInput, User): Payment`) y adapter `ManualPaymentGateway` con constante `CODE='manual'`. DTO readonly `ManualPaymentInput(amountMinor, currency, providerReference, method, occurredAt, notes?)`. Los métodos `createCheckout/getPayment/verifyWebhook/refund` para gateways reales quedan **explícitamente diferidos** (comentario referencia verbatim WORKFLOWS.md). No se implementa Stripe, Mercado Pago, PayPal, tarjeta, checkout público, webhook real, renovación automática, cobro recurrente, cambio automático de plan, prorrateo, cupones, CFDI ni conciliación bancaria automática — todo fuera del MVP.
- Actions de dominio en `App\Actions\Commerce\`:
  - `CreateOrder($customer, $planVersion, $idempotencyKey, $concept=null, $createdBy=null): Order`. Idempotencia por `orders.idempotency_key` UNIQUE + lookup bajo `lockForUpdate` (mismo payload devuelve la existente; distinto payload → `ORDER_IDEMPOTENCY_CONFLICT`). Congela `total_minor` y `currency` desde la `PlanVersion` publicada; publicar una versión posterior más cara **NO** modifica orders previas. Rechaza `ORDER_PLAN_VERSION_NOT_PUBLISHED` a nivel Action y a nivel trigger BD.
  - `CancelOrder($order, $actor): Order`. `lockForUpdate`; sólo transiciona `pending→cancelled`. Rechaza `ORDER_NOT_CANCELLABLE` para `paid|cancelled|refunded`.
  - `RecordManualPayment` (delegación a `ManualPaymentGateway`): `DB::transaction` + `lockForUpdate` sobre order + lookup existente por `(provider, provider_reference)` bajo lock. Códigos: `ORDER_NOT_FOUND`, `PAYMENT_IDEMPOTENCY_CONFLICT` (misma referencia con order/monto/moneda distintos), `ORDER_CANCELLED_CANNOT_PAY`, `ORDER_REFUNDED_CANNOT_PAY`, `PAYMENT_AMOUNT_MISMATCH`, `PAYMENT_CURRENCY_MISMATCH` (con `strcasecmp`). Crea `Payment` en estado `succeeded`, setea `confirmed_by`; si order estaba `pending` la marca `paid` con `paid_at`.
  - `ActivatePaidOrder($order): Subscription`. **Idempotencia crítica** vía `orders.activated_subscription_id`: reprocesar una order pagada no crea segunda Subscription/SubscriptionPeriod ni duplica derechos. `lockForUpdate` sobre order → exige `paid|refunded` (`ORDER_NOT_PAID`) → si `activated_subscription_id` seteado devuelve la Subscription existente → `lockForUpdate` sobre customer → verifica ausencia de suscripción operativa (`CUSTOMER_ALREADY_HAS_ACTIVE_SUBSCRIPTION`, verbatim brief §11: sin upgrade/downgrade automático) → invoca `CreateSubscription` + `OpenSubscriptionPeriod` → escribe `orders.activated_subscription_id`, `orders.subscription_period_id`, `subscription_periods.paid_order_id`. Fechas del primer periodo con `Carbon::addMonths|addYears` según `PlanVersion.interval_unit × interval_count` (brief §12: sin aproximar mes = 30 días). Ventana `[paid_at, paid_at + interval)`.
  - `RecordManualRefund($payment, $amountMinor, $reason, $idempotencyKey, $confirmedBy, $providerReference=null): Refund`. Idempotencia por `refunds.idempotency_key` UNIQUE. `lockForUpdate` sobre payment; exige `succeeded` (`PAYMENT_NOT_SUCCEEDED`). Agrega `SUM(refunds succeeded) + nuevo ≤ payment.amount_minor` (`REFUND_EXCEEDS_PAYMENT`). Crea Refund `succeeded` con `completed_at`. Si total refundado == amount y order estaba `paid` → transición `paid→refunded`. Códigos: `REFUND_INVALID_AMOUNT`, `REFUND_IDEMPOTENCY_CONFLICT`, `REFUND_EXCEEDS_PAYMENT`. **NUNCA devuelve unidades del ledger** (verbatim brief §14): reservas `consumed` siguen consumidas tras un refund total; el reintegro operativo se hace por Action manual futura.
- Políticas: `OrderPolicy` (admin viewAny/create; cliente view propio; update/delete=false — mutación exclusiva vía Actions), `PaymentPolicy` (idéntico patrón; create=false, se registra desde Order), `RefundPolicy` (admin-only), `PaymentEventPolicy` (admin view-only; create/update/delete=false — se ingestan por gateway/webhook, nunca por Filament).
- Recursos Filament en `/admin`:
  - `OrderResource`: form con `Cliente`, `Plan` (`->live()` filtra versiones publicadas del plan), `Versión de plan publicada` (`v{n} · precio`), `Concepto`, `Clave de idempotencia` (default UUID), todo `disabledOn('edit')`. Página `CreateOrder` override `handleRecordCreation` para invocar la Action `CreateOrder`. Tabla con badge de estado (`pending`=warning, `paid`=success, `cancelled|refunded`=danger), totales formateados y columna `Suscripción` con `#id`. **Row actions**: (a) `Registrar pago manual` (visible sólo con status=pending, form con `provider_reference|method|occurred_at`, invoca `RecordManualPayment` con amount/currency tomados de la order), (b) `Activar suscripción` (visible con status=paid AND activated_subscription_id=null, invoca `ActivatePaidOrder`), (c) `Cancelar pedido` (visible con status=pending). Notificaciones Filament reflejan el código semántico devuelto por la Action.
  - `PaymentResource` (`canCreate=false`): tabla con badge de estado, importes formateados, columna `provider_reference` con `limit(24) + tooltip`. Row action `Registrar devolución` en payments `succeeded` (form `amount_minor|reason|provider_reference|idempotency_key` → `RecordManualRefund`).
  - `RefundResource` (`canCreate=false`): tabla lectura con badge y montos.
  - `PaymentEventResource` (`canCreate=false`): tabla lectura mostrando `event_id` con tooltip completo, badge de estado y timestamps `received_at/processed_at`.
  - Registrados en `AdminPanelProvider` en ese orden.
- Factories `OrderFactory`, `PaymentFactory`, `RefundFactory`, `PaymentEventFactory`. `OrderFactory::forPlanVersion(PlanVersion)` estado helper para tests. `OrderFactory` publica automáticamente la `PlanVersion` que crea para satisfacer el trigger BD.
- Tests añadidos (28 tests, 68 assertions netas nuevas) bajo `tests/Feature/Commerce/`:
  - `OrderLifecycleTest` (7): creación admin congela precio/moneda; publicar nueva versión posterior más cara no modifica order previa; PlanVersion borrador rechazada; misma `idempotency_key` con mismo payload devuelve la misma order; misma key con payload distinto → `ORDER_IDEMPOTENCY_CONFLICT`; cancelar `pending → cancelled`; cancelar `paid` → `ORDER_NOT_CANCELLABLE`.
  - `ManualPaymentTest` (6): pago manual válido marca order `paid`; `PAYMENT_AMOUNT_MISMATCH`; `PAYMENT_CURRENCY_MISMATCH`; misma referencia devuelve el mismo payment; misma referencia con order/monto distintos → `PAYMENT_IDEMPOTENCY_CONFLICT`; segundo `succeeded` en la misma order rechazado por el **índice único parcial** `payments_order_succeeded_uniq` (`QueryException` desde BD, evidencia del guardrail concurrente).
  - `ActivatePaidOrderTest` (6): activación crea Subscription + primer SubscriptionPeriod con `entitlement_snapshot.planning_limit=4`, ventana `[paid_at, paid_at + 1 mes)` con `Carbon::addMonth`, `paid_order_id` apunta a la order y order refleja `activated_subscription_id` + `subscription_period_id`; reactivación idempotente devuelve la misma Subscription y no crea segundo periodo; order no pagada → `ORDER_NOT_PAID`; cliente con suscripción activa → `CUSTOMER_ALREADY_HAS_ACTIVE_SUBSCRIPTION` sin dejar `activated_subscription_id`; publicar nueva PlanVersion posterior no altera la Subscription/entitlement histórica; **rollback transaccional** — se inyecta un `OpenSubscriptionPeriod` que lanza excepción y se verifica que no queda Subscription parcial ni `orders.activated_subscription_id` seteado.
  - `RefundTest` (6): refund total marca order `refunded`; refund parcial mantiene order `paid`; suma que excede → `REFUND_EXCEEDS_PAYMENT`; idempotencia con misma key + payload; `REFUND_IDEMPOTENCY_CONFLICT` con payload distinto; **refund NO libera reservas consumidas** (verbatim brief §14): tras `ActivatePaidOrder` + `ReservePlanningUnits(2)` + `ConsumePlanningReservation`, el refund total deja la reserva en `consumed` y `SubscriptionBalance.consumed=2`.
  - `PaymentEventDedupeTest` (3): UNIQUE `(provider, event_id)` rechaza inserción duplicada (`QueryException`); CHECK `processed_at_consistency` rechaza `status=processed` con `processed_at=null` a nivel BD; flujo completo cruzado `RecordManualPayment` + `PaymentEvent` referenciando el mismo `provider_reference` sin choque.
  - Nota metodológica: PHPUnit `RefreshDatabase` envuelve el test en transacción y **no permite** dos conexiones Postgres visibles entre sí; los escenarios de "dos intentos simultáneos" del brief §20 se cubren con los guardrails BD (UNIQUE `(provider, provider_reference)`, partial UNIQUE `payments_order_succeeded_uniq`, UNIQUE `orders.idempotency_key`, UNIQUE `refunds.idempotency_key`) y con `SELECT FOR UPDATE` en las Actions.
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` ⇒ `OK (203 tests, 534 assertions)` en ~43 s (28 tests nuevos vs los 175 previos). Ninguna regresión.
- Auditoría responsive completada 2026-09-08 con `php artisan serve --port=8080`, cuenta `admin@example.test`, navegador Playwright integrado. HTTP 200 en las 4 rutas nuevas para 375×812 y 1440×900: `/admin/orders`, `/admin/payments`, `/admin/refunds`, `/admin/payment-events`. Sidebar colapsable en móvil (`Expandir barra lateral`), tabla sin overflow horizontal, columnas prioritarias visibles, badges de estado legibles. `/admin/orders/create` renderiza con Select cliente + Plan `->live()` que filtra versiones publicadas + campo `Clave de idempotencia` pre-poblado con UUID.
- **Diferimientos explícitos de 3C** (no fueron implementados en esta subfase, verbatim brief):
  - Enganche `Order → PlanningRequest`: `orders.subscription_period_id` está nullable y se usa como puntero informativo tras `ActivatePaidOrder`; la reserva de unidades por autorización de `PlanningRequest` se implementa en 3D; el consumo queda para Fase 4.
  - Renovación automática, cobro recurrente y cambio automático de plan.
  - Upgrade/downgrade de suscripción: brief §11 exige rechazar con `CUSTOMER_ALREADY_HAS_ACTIVE_SUBSCRIPTION` (implementado).
  - Checkout público, formulario de tarjeta, redirect a gateway, webhook real, integración Stripe/Mercado Pago/PayPal, conciliación bancaria automática, CFDI, cupones, prorrateo.
  - Devolución automática de unidades tras refund (brief §14: sólo por regla explícita futura).
- **Subfase 3D y Fase 4 NO iniciadas**: no se implementó integración `PlanningRequest ↔ UsageReservation`, `client_correction` como recurso operativo, `ReviewerAssignment`, revisión humana operacional, IA, generación, documentos, renderer DOCX/PDF, notificaciones comerciales, máquina de estados de pipeline ni outbox.

### Evidencia Subfase 3D

Esta sección conserva la evidencia del cierre original de 3D. Las guardas de PostgreSQL se refuerzan posteriormente en **3D-H**, descrita abajo; la descripción original de los BEFORE triggers no equivale a la validación diferida final.

- Migración `2026_09_15_000001_add_commercial_authorization_to_planning_requests.php`:
  - Añade a `planning_requests` las columnas comerciales `subscription_id`, `subscription_period_id`, `plan_version_id`, `planning_days`, `planning_units`, `calculation_strategy`, `calculation_snapshot JSONB`, `correction_limit_snapshot`, `human_review_required_snapshot`, `commercial_authorized_at`. **FK compuestas defensivas**: `(subscription_id, owner_id) → subscriptions(id, customer_id)` garantiza a nivel BD que la Order/Subscription/Period pertenece al mismo cliente; `(subscription_period_id, subscription_id, plan_version_id) → subscription_periods(id, subscription_id, plan_version_id)` bloquea manipulación cruzada de identidad periodo/plan_version.
  - CHECK `planning_commercial_complete` obliga que las 9 columnas comerciales estén NULL antes de autorizar y **todas** presentes tras autorizar (`calculation_strategy='calendar_days_v1'`, `jsonb_typeof(calculation_snapshot)='object'`, `planning_days>0`, `planning_units>0`, `correction_limit_snapshot>=0`).
  - Nueva tabla `planning_request_segments` con `sequence`, `starts_on`, `ends_on`, `calendar_days`, `units=1`. CHECK `planning_segments_dates` = `ends_on>=starts_on AND calendar_days=ends_on-starts_on+1 AND sequence>0 AND units=1`. **EXCLUDE USING gist** sobre `daterange(starts_on, ends_on, '[]')` prohíbe solapes por `planning_request_id`. UNIQUE `(planning_request_id, sequence)`.
  - Trigger PL/pgSQL `planning_segment_guard_trg` (INSERT/UPDATE/DELETE): bloquea DELETE/UPDATE tras `commercial_authorized_at IS NOT NULL` (`PLANNING_SEGMENTS_IMMUTABLE`), impide cambiar `planning_request_id`, exige que la request esté en `ESPERANDO_PAGO` y que el rango del segmento esté dentro de `[request.starts_on, request.ends_on]` (`PLANNING_SEGMENT_INVALID_RANGE`).
  - Trigger PL/pgSQL `planning_commercial_guard_trg`: (a) bloquea DELETE de solicitudes autorizadas (`PLANNING_COMMERCIAL_IMMUTABLE`); (b) tras autorización, cualquier mutación de las 14 columnas críticas (`subscription_id`, `subscription_period_id`, `plan_version_id`, `planning_days`, `planning_units`, `calculation_strategy`, `calculation_snapshot`, `correction_limit_snapshot`, `human_review_required_snapshot`, `commercial_authorized_at`, `starts_on`, `ends_on`, `owner_id`, `group_id`) → `PLANNING_COMMERCIAL_IMMUTABLE`; (c) en la transición `commercial_authorized_at NULL→NOT NULL` la BD valida que `status` pase de `ESPERANDO_PAGO` a `LISTA_PARA_PROCESAR`, que `input_snapshot`+`current_version_id` estén presentes, que `planning_units = ceil(planning_days / max_planning_days)`, que el conteo y la suma de segmentos correspondan **exactamente** con `planning_units` y `planning_days`, y que cada segmento tenga la posición `starts_on + (seq-1)*max_days` sin exceder `ends_on` (`PLANNING_COMMERCIAL_INVALID_SEGMENTS`, `PLANNING_COMMERCIAL_INVALID_STATE`). No se puede autorizar sin snapshot, sin segmentos o con U mal calculada aunque un cliente omita la Action.
- Modelo `App\Models\PlanningRequestSegment` (fillable + casts). Modelo `PlanningRequest` amplía casts (`calculation_snapshot:array`, `commercial_authorized_at:datetime`, `human_review_required_snapshot:bool`, `planning_days:int`, `planning_units:int`, `correction_limit_snapshot:int`) y relaciones `subscription`, `subscriptionPeriod`, `planVersion`, `segments()->orderBy('sequence')`, `usageReservations`.
- Servicio `App\Services\Commerce\CurrentCommercialRights::forCustomer($user, bool $lock=false)`: resuelve server-side el par `Subscription active + SubscriptionPeriod active` con `starts_at <= now < ends_at`. Rechaza `past_due|pending|cancelled|expired`, periodos futuros, expirados o `ended`, y periodos cuyo `plan_version_id` no coincida con la subscription. En autorización se usa con `lockForUpdate` para serializar competencia por saldo.
- Action `App\Actions\Planning\CalculatePlanningCommercialRequirements`: envoltura pura del `PlanningUnitCalculator` (`calendar_days_v1`, `U=ceil(D/M)` con días naturales inclusivos) que retorna `{planning_days, planning_units, strategy, segments[]}`. Mapea segmentos internos a la forma persistible `{sequence, starts_on, ends_on, calendar_days, units:1}`. Método `forDates()` sirve para previews en el wizard sin escribir en BD.
- Excepción semántica `App\Exceptions\PlanningCommercialException(code, ?needed, ?available)` con `userMessage()` que traduce `COMMERCIAL_RIGHTS_REQUIRED`, `INSUFFICIENT_PLANNING_UNITS`, `INSUFFICIENT_HUMAN_REVIEW_UNITS`, `GROUP_LIMIT_EXCEEDED`, `PLANNING_GROUP_ARCHIVED` a copy para docentes sin nombres técnicos.
- Action clave `App\Actions\Planning\AuthorizePlanningRequestForProcessing::execute($actor, $request)`:
  - `DB::transaction(attempts:3)` + `lockForUpdate` sobre `planning_requests` y `users`.
  - `Gate::forUser($customer)->authorize('authorizeProcessing', $fresh)` — sólo el owner activo y verificado con rol `Customer`.
  - Guardia de reentrada: si `commercial_authorized_at !== null` retorna la solicitud sin efectos secundarios (**idempotencia total**).
  - Guardia de estado: requiere `status=ESPERANDO_PAGO`, `curriculum_confirmed_at NOT NULL`, `currentInputVersion` que coincide con `input_snapshot` **y con las fechas actuales** `starts_on/ends_on`. Cualquier discrepancia → `PLANNING_REQUEST_NOT_CONFIRMED`. Esto cierra el defecto de "confirmar con datos anteriores".
  - `Group::where('owner_id=customer)->lockForUpdate()->get()` (incluye archivados para bloquear unarchive concurrente).
  - `CurrentCommercialRights::forCustomer($customer, lock:true)` → `COMMERCIAL_RIGHTS_REQUIRED` si no hay periodo vigente. **NUNCA acepta `past_due`** para nuevos derechos.
  - Usa `$period->planVersion` (**PlanVersion histórica**, no la publicada más reciente).
  - Cuenta grupos NO archivados: `count > group_limit → GROUP_LIMIT_EXCEEDED`; `group_id de la request archivado → PLANNING_GROUP_ARCHIVED`. Los grupos no se modifican.
  - Calcula `{planning_days, planning_units, segments}` con el calculador; valida `units <= balance.planning.available()` → `INSUFFICIENT_PLANNING_UNITS`.
  - `ReservePlanningUnits($period, Planning, $units, "planning-request:{$id}:planning", $requestId)`. Si el snapshot marca `human_review_required=true`, valida `units <= balance.human_review.available()` → `INSUFFICIENT_HUMAN_REVIEW_UNITS`; si pasa, reserva `HumanReview` con clave `"planning-request:{$id}:human-review"`. **Atomicidad**: al estar dentro de la misma transacción, si la segunda reserva falla, la primera se revierte junto con el snapshot y los segmentos → no queda planning reservado sin human_review.
  - Congela `calculation_snapshot` con `{schema_version:1, plan, plan_version{checksum}, subscription, subscription_period{starts_at,ends_at}, entitlements, planning_days, planning_units, strategy, segments}`.
  - `segments()->createMany($calculation['segments'])` — los guardrails BD comprueban rango y sin huecos.
  - `forceFill` de `subscription_id`, `subscription_period_id`, `plan_version_id`, `planning_days`, `planning_units`, `calculation_strategy`, `calculation_snapshot`, `correction_limit_snapshot` (del entitlement), `human_review_required_snapshot`, `commercial_authorized_at=now()`, `status=LISTA_PARA_PROCESAR`, `lock_version+=1`.
  - Escribe **un único** `RequestStateEvent` con `from=ESPERANDO_PAGO`, `to=LISTA_PARA_PROCESAR`, `reason='commercial_authorization_reserved'`.
  - NO inicia `GENERACION_IA`, no toca el pipeline de la Fase 4.
- Idempotencia de reserva: `ReservePlanningUnits` amplía su verificación de conflicto para incluir `planning_request_id` — dos requests distintas que casualmente compartieran claves (no debería ocurrir con la convención `planning-request:{id}:{tipo}`) también fallarían con `USAGE_IDEMPOTENCY_CONFLICT` si intentaran suplantarse.
- Policy `PlanningRequestPolicy::authorizeProcessing(User, PlanningRequest)`: `isActiveCustomer($user) && $request->owner_id === $user->id`. Bloquea admin/reviewer/otros customers explícitamente (verificado en test).
- Página Filament `/app/mi-plan` (`App\Filament\App\Pages\MyPlan`, slug `mi-plan`): sólo `Customer` activo con email verificado. Presenta `PlanningCommercialPresentation::forCustomer($user)` — nombre del plan, ventana del periodo, `max_planning_days`, saldo `planning{consumed,reserved,available}`, `human_review` cuando aplica, `group_limit`, `correction_limit`. Sin nombres técnicos. Vista Blade `commercial-summary.blade.php` reutilizada por el wizard y por la vista de solicitud confirmada.
- `PlanningRequestResource` (panel `/app`): añade `Placeholder::commercial_summary` en el paso "Confirmar" del wizard con preview live basado en `starts_on/ends_on` seleccionados. Columna de estado en tabla usa `PlanningCommercialPresentation::status()` → "Borrador" / "Pendiente de activar" / "Preparando".
- Página `EditPlanningRequest`: acción "Confirmar planeación" ejecuta **primero** `$this->save(shouldRedirect:false, shouldSendSavedNotification:false)` — esto dispara la validación del formulario y persiste `starts_on/ends_on/selección` **actuales visibles** vía `UpdatePlanningRequestDraft` + `SyncPlanningRequestSelections` — **después** invoca `ConfirmPlanningRequest` (congela `input_snapshot`) — **y por último** `attemptCommercialAuthorization()`. Cierra el defecto de "autorización con datos anteriores". `databaseTransaction(false)` evita anidar transacciones sobre Actions que ya usan la suya.
- Página `ViewPlanningRequest` (panel `/app`): infolist muestra estado amigable, unidades reservadas, `correction_limit` incluido, "Revisión humana incluida: Sí/No"; y —cuando `status=ESPERANDO_PAGO`— renderiza el mismo `commercial-summary`. Header action "Activar procesamiento" invoca `AuthorizePlanningRequestForProcessing` — clave para el flujo *retry*: confirmé sin plan → activo Subscription más tarde → pulso "Activar procesamiento" → reserva y transiciona a `LISTA_PARA_PROCESAR`. Las notificaciones de error usan `PlanningCommercialException::userMessage()` (nunca códigos crudos).
- Recurso admin `App\Filament\Resources\PlanningRequests\PlanningRequestResource` (nuevo, list/view **sin edit**): permite auditar snapshot comercial de cualquier solicitud (`Solo lectura` en la vista); ruta `/admin/planning-requests/{id}/edit` responde 404 vía `getPages()`.
- Factory `PlanningRequestSegmentFactory`. Seeder demo `Database\Seeders\Phase3dDemoSeeder` (gated a `local|testing` + `PHASE3D_DEMO_PASSWORD` env de 12+ chars) crea 4 escenarios (`basic|reviewed|short|none`) × 2 viewports (`mobile|desktop`) con cuentas `qa3d-*@example.test` para auditoría manual sin exponer datos reales.
- Tests añadidos (34 tests nuevos, +322 assertions vs 3C):
  - `PlanningCommercialAuthorizationTest` (25 casos, incluidos data providers):
    - `#[DataProvider durations]` — U para D∈{1,7,8,14,15,28,31} con M=7 verifica U={1,1,2,2,3,4,5} y segmentos exactos consecutivos sin solape, todos ≤M días.
    - `test_missing_rights_preserves_confirmation_and_later_retry_authorizes` — confirmación queda persistida sin reserva; posterior activación reserva y transiciona.
    - `test_reviewed_reserves_both_resources_atomically_and_corrections_are_per_request` — REVIEWED reserva `planning` + `human_review` con misma quantity, `correction_limit_snapshot=2` (nunca U×correction_limit), sin reservar `client_correction`.
    - `test_human_shortage_rolls_back_planning_reservation` — precarga 7 human_review, autoriza con U=4, falla `INSUFFICIENT_HUMAN_REVIEW_UNITS`, verifica que `planning.available()` volvió a 8 (rollback total).
    - `test_planning_shortage_reports_required_and_available_without_writes` — `INSUFFICIENT_PLANNING_UNITS` con needed=4/available=3, cero writes.
    - `test_group_limit_blocks_until_customer_archives_an_excess_group` — 2 grupos activos con `group_limit=1` → `GROUP_LIMIT_EXCEEDED`, no archiva automáticamente; cliente archiva 1 y reintenta → autoriza.
    - `test_draft_and_missing_snapshot_cannot_be_authorized` — `BORRADOR` y `ESPERANDO_PAGO` con `curriculum_confirmed_at=null` rechazados.
    - `test_historical_version_and_idempotent_retry_survive_new_version_and_expiry` — publica PlanVersion v2 (M=28) antes de autorizar, autoriza con la versión de la suscripción, publica v3 y cambia el nombre del plan; espera 40 días → repetir autorización devuelve `getAttributes()` idénticos, 1 sola reserva, 2 estados (no duplica el evento), 4 segmentos, y `plan_version_id/checksum` siguen apuntando a la versión histórica.
    - `#[DataProvider invalidRights]` × 9 — `past_due`, `cancelled`, `expired`, `pending`, `period-ended`, `period-expired`, `period-future`, `subscription-future`, `subscription-ended` todos → `COMMERCIAL_RIGHTS_REQUIRED`.
    - `test_policy_allows_only_verified_active_owner_and_not_administrator` — otro customer, admin, reviewer rechazados; owner suspendido rechazado.
    - `test_postgresql_prevents_authorized_snapshot_and_segment_mutations` — 4 mutaciones directas (`planning_units=1`, update segmentos, delete segmentos, insertar segmento nuevo) todas → `IMMUTABLE`.
  - `PlanningCommercialConcurrencyTest::test_two_real_transactions_compete_for_last_units_and_only_one_authorizes` — **PostgreSQL real, sin mocks**: `saldo=2`, dos requests que necesitan 2 cada una; el test main abre transacción y toma `lockForUpdate` sobre el `subscription_period`, luego lanza dos procesos PHP hijo (`Symfony\Process`) que ejecutan la Action independientemente. Espera hasta ver `wait_event_type='Lock'` en `pg_stat_activity` para los 2 PIDs → COMMIT del lock → ambos procesos compiten. Verifica que las salidas son exactamente `[AUTHORIZED, INSUFFICIENT_PLANNING_UNITS]`, que solo 1 request quedó `LISTA_PARA_PROCESAR`, la otra sigue `ESPERANDO_PAGO`, hay solo 1 fila en `usage_reservations` (quantity=2), 2 `planning_request_segments` (todas de la ganadora), 1 `request_state_events(to_status=LISTA_PARA_PROCESAR)`, cero `consumed_at`. Overrides `refreshDatabase()` para usar `migrate:fresh` sin `RefreshDatabase` (los procesos hijos necesitan una BD comprometida, no una transacción atada al test).
  - `PlanningCommercialUiTest` (8 casos Livewire): confirmación sin derechos preserva `ESPERANDO_PAGO` sin reservas → reintento tras activar Subscription reserva y transiciona; confirmación con derechos reserva y muestra "Preparando" sin exponer `LISTA_PARA_PROCESAR` ni `operation_key`; **confirmar con `ends_on` editado y sin guardado explícito congela las fechas nuevas y calcula U=2**; admin puede leer snapshot comercial en `/admin/planning-requests/{id}` (con "Solo lectura", `calendar_days_v1`) pero `/edit` responde 404; datos maliciosos en `activateProcessing.data` (`subscription_period_id`/`plan_version_id`/`planning_units`/`operation_key` ajenos) son ignorados — se derivan server-side; otro customer / reviewer no abren la solicitud ni `/app/mi-plan` (403), no auth redirige a `/app/login`; `/app/mi-plan` muestra el plan propio (`4 reservadas`, `Revisión humana incluida`), escapa XSS en el nombre del plan (`&lt;script&gt;`), no ve el plan de otra cuenta ni nombres internos; preview del wizard (`CreatePlanningRequest`/`EditPlanningRequest`) muestra `4 unidades necesarias`, `8 disponibles`, `Revisión humana incluida` — sin escribir `calculation_snapshot` ni crear reservas.
- Suite completa: `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` ⇒ `OK (237 tests, 856 assertions)` en ~68 s (34 tests nuevos + +322 assertions vs 3C).
- Evidencia visual histórica reportada para 3D (no repetida ni certificada por 3D-H): auditoría responsive del 2026-09-08 con `php artisan serve --port=8080` sirviendo la BD sembrada por `DemoPlansSeeder` + `Phase3dDemoSeeder` (cuentas `qa3d-*@example.test`, contraseña vía `PHASE3D_DEMO_PASSWORD`). Playwright integrado, `document.documentElement.scrollWidth > clientWidth` = `false` en las 5 rutas de `/app` de 3D en 375×812 y 1440×900: `/app`, `/app/mi-plan`, `/app/planning-requests`, `/app/planning-requests/create`, `/app/planning-requests/{id}/edit`. Escenarios verificados manualmente: (a) `qa3d-none-mobile` → `/app/mi-plan` muestra "No tienes un plan activo con un periodo vigente"; (b) `qa3d-short-mobile` (planning_limit=2) → "Unidades de planeación: 0 usadas · 0 reservadas · 2 disponibles" + "Este plan no incluye revisión humana"; (c) `qa3d-reviewed-mobile` → "Revisión humana incluida. 0 usadas · 0 reservadas · 8 disponibles", "Grupos activos permitidos: 1. Rondas de corrección por solicitud: 2". Todos los overflows programáticos = `false`, ninguna consola marcó `livewire-error`, sidebar colapsable en móvil, textos sin nombres técnicos (`operation_key`, `subscription_period_id` no aparecen).
- Laravel Boost 2.8 documentado como dependencia dev en `composer.json` con `boost.json` (`{"agents":["codex","claude_code"],"guidelines":true}`) según `AGENTS.md`. Laravel y Filament no fueron actualizados.
- **Diferimientos explícitos de 3D** (no fueron implementados, verbatim brief):
  - Consumo real de unidades (`reserved → consumed`) al iniciar generación IA — se hará en Fase 4.
  - `client_correction` como recurso operativo (por-solicitud) enlazado a rondas de corrección de un `DocumentVersion` — Fase 4.
  - Renovación / cancelación de periodo desde el panel del docente y UI de pagos propios — se difieren.
- **Fase 4 NO iniciada**: no se implementó máquina de estados de pipeline, transiciones fuera de `LISTA_PARA_PROCESAR`, generación IA, `PromptTemplate`, `PromptVersion`, contratos IA, `Document`, `DocumentVersion`, snapshot curricular confirmado en el documento, cobertura de segmentos, `ReviewerAssignment`, revisión humana operacional, corrección por sección, renderer DOCX/PDF, outbox ni notificaciones comerciales.


### Evidencia Subfase 3D-H — Hardening de integridad (2026-09-09)

- [x] Hardening implementado y verificado en PostgreSQL real: **281 tests / 1002 assertions**, sin fallos; Fase 4 continúa pendiente.
- Estado inicial inspeccionado mediante `git status`, `git diff`, `git diff --staged` y `git log -5 --oneline --decorate`: `main` en `142e09c`, árbol limpio, sin cambios previos que conservar o descartar. No se modifican dependencias ni tags.
- Recuperación del entorno original: README, `tools/php.ps1`, configuración PHP/Composer/PHPUnit/env, TASKS e historial (`git grep "55432"`, `git log -S"55432" --all --oneline`) identifican una instancia secundaria local, no Docker/CI/túnel. `postgresql.conf` fija `127.0.0.1:55432`; `postmaster.opts` apunta a `.runtime/postgresql/pgsql/bin/postgres.exe -D .runtime/pgdata`. El primer `pg_ctl start` falló dentro del sandbox con error 87 al crear el token restringido. Repetido con autorización fuera del sandbox arrancó **el mismo clúster**, sin recrearlo ni cambiar `.env.testing`.
- Comandos de entorno ejecutados: `pg_ctl.exe -D .runtime/pgdata -l .runtime/postgresql.log -w start` → `server started`; `pg_isready.exe -h 127.0.0.1 -p 55432` → `accepting connections`; consulta SQL con credenciales de `.env.testing` → `current_database=planeaciones_test`, `current_user=planeaciones`, puerto `55432`, PostgreSQL `18.6`. No se apuntaron pruebas a 5432 ni a la base de desarrollo.
- Versiones comprobadas con el wrapper PHP y `composer show --direct`: PHP 8.4.7, Laravel 13.31.0, Filament 5.8.1 y PHPUnit 12.5.34, sin actualizaciones.
- Migración nueva `2026_09_16_000001_harden_planning_commercial_integrity.php`, ordenada después de la migración existente de 3D. Mantiene intactas las migraciones anteriores. Sustituye los BEFORE de solicitud/segmentos por guardas históricas y añade `planning_commercial_integrity_trg`, `planning_segments_integrity_trg` y `planning_reservations_integrity_trg`, los tres `DEFERRABLE INITIALLY DEFERRED`. La comprobación al commit exige estado/autorización, snapshot e identidad coherentes, D/U, segmentos y reservas correspondientes. Valida también datos preexistentes al aplicar la migración.
- Ledger: `usage_reservations_identity_trg` impide DELETE y cambios de identidad económica; conserva el trigger de transiciones y CHECK de timestamps de 3B. Periodos: `subscription_periods_history_trg` congela identidad y entitlement desde creación, incluido `pending`; permite estado y enlace posterior con pedido. Decisiones y clasificación de estados en `DATABASE.md`.
- `PlanningCommercialIntegrityTest` usa commits reales, sin la transacción envolvente de `RefreshDatabase`. Cubre INSERT sin autorización; todos los estados posteriores definidos; padre incompleto al commit; padre primero con hijos/evento posteriores; snapshot adulterado; reservas ausentes, de otro periodo, con cantidad/clave incorrecta o liberadas; DELETE/reasignaciones del ledger; estados consumed/released permitidos donde corresponde; periodo pendiente inmutable y activable; estado posterior `GENERACION_IA` por SQL con historial válido. Cada caso ejecuta `migrate:fresh` y `migrate:rollback` en la BD de pruebas; ambos mecanismos pasaron.
- Comandos de pruebas ejecutados con `.\tools\php.ps1 vendor/phpunit/phpunit/phpunit` y `--do-not-cache-result`:
  - Baseline, además `--stop-on-error`: **237 tests / 856 assertions**, todos pasan.
  - `--filter PlanningCommercialAuthorizationTest`: **25 tests / 247 assertions**, todos pasan.
  - `--filter PlanningCommercialConcurrencyTest`: **1 test / 11 assertions**, pasa con dos procesos PostgreSQL reales bloqueados; una autorización, una insuficiencia de saldo y cero consumo.
  - `--filter PlanningCommercialIntegrityTest`: **44 tests / 146 assertions**, todos pasan. Un primer intento falló por comparar el orden de claves de JSONB contra el array previo a persistir; se corrigió la aserción leyendo el snapshot persistido y se repitió satisfactoriamente.
  - Suite completa final, sin filtro: **281 tests / 1002 assertions**, todos pasan en 150.598 s, incluidos autorización, UI y concurrencia real.
- `.\tools\php.ps1 vendor/bin/pint --dirty --format agent`: ejecutado; corrigió imports y estilo del nuevo test. No se reformatearon fases previas.
- `git diff --check`: ejecutado, código de salida 0, sin errores de whitespace.
- Correcciones documentales: UI tiene 8 casos, autorización tiene 25 contando providers; el test histórico publica v2 antes de autorizar y v3 después; 3D reserva y el consumo sigue diferido. Evidencia visual original identificada como histórica, sin atribuirle una nueva verificación en 3D-H.
- **Fase 4 NO iniciada**. Ningún consumo desde PlanningRequest, job, IA, renderer, documento, asignación de revisión, outbox o notificación nueva. Los cambios de estado y consumo directos existen solo en las pruebas aisladas de compatibilidad.

## Fase 4 — Pipeline vertical manual

- [x] **Subfase 4A — contratos canónicos y prompts.** Commit `ceb4ea8`: schemas `GeneratedPlanDraftV1`/`CanonicalPlanV1`, DTOs, validadores, `CanonicalPlanAssembler`, contratos de servicios, PromptTemplate/PromptVersion, AIExecution mínimo y administración `/admin`.
  - Verificación local PostgreSQL/Laravel ejecutada tras aplicar el patch: `GeneratedPlanDraftValidatorTest` **10 tests / 15 assertions**, `PromptRendererTest` **4 / 5**, `tests/Feature/AI` **36 / 77** y suite completa **331 tests / 1099 assertions**, todos sin fallos. `git diff --check` limpio antes del commit.
  - 4A no cambia estados del pipeline, no consume reservas, no registra adapter LLM y no realiza HTTP externo.
  - Contrato curricular: proveedor solo referencia códigos; textos/coverage se ensamblan desde `RequestInputVersion` congelado.

- [x] **Subfase 4B — arranque transaccional y outbox manual.** Commit `81b7da0`, verificado localmente sobre PostgreSQL/Laravel antes del cierre.
  - `DispatchPlanningGeneration`: única transición nueva `LISTA_PARA_PROCESAR → GENERACION_IA`, `RequestStateEvent` de sistema, `AiExecution` generation/manual y `outbox_events` en la misma transacción. Consume **solo** la reserva `planning`; `human_review` permanece `reserved`. Repetición devuelve la misma ejecución y no duplica consumo/evento/outbox.
  - `request_blocks` materializa bloqueo técnico separado del estado; `outbox_events` tiene event_key único, lease recuperable, intentos y payload inmutable; `ai_manual_packages` registra el paquete privado exacto por ejecución y es inmutable. Un constraint trigger diferido impide que SQL directo coloque una solicitud autorizada en `GENERACION_IA` o estados posteriores sin consumo `planning`, `AiExecution`, evento `LISTA_PARA_PROCESAR→GENERACION_IA` y outbox coherentes.
  - `ai:process-outbox` reclama eventos pendientes, renderiza el PromptVersion publicado, escribe paquete JSON en disco privado fuera del lock de BD, cambia ejecución a `waiting_manual` y publica el evento. Error técnico mantiene `GENERACION_IA`, abre `ai_failed` sanitizado y deja evento reintentable.
  - `AI_MODE=api` sigue rechazado explícitamente (`AI_API_PROVIDER_NOT_CONFIGURED`): no hay proveedor HTTP, tokens, modelo ficticio, resultado IA, DocumentVersion ni auditoría. Integración API continúa reservada para Fase 7.
  - Verificación de cierre: `PlanningRequestStateMachineTest` **2 tests / 5 assertions**, `PlanningGenerationDispatchTest` **11 / 55**, `ManualGenerationOutboxTest` **9 / 54**, `PlanningGenerationIntegrityTest` **3 / 14**, `PlanningCommercialIntegrityTest` **44 / 146** y suite completa **356 tests / 1227 assertions**, todos sin fallos. `git diff --check` limpio.
  - Durante la validación local se corrigió una falsa divergencia de `input_manifest` causada únicamente por el orden de claves de PostgreSQL JSONB; la comparación usa ahora hash de `CanonicalJson`, conservando igualdad semántica sin debilitar el manifest.

- [x] **Subfase 4C — importación manual de generación y DocumentVersion.** Commit `be911e4`, tag `phase-4c-complete`.
  - `ai:import-generation-result`: valida `GeneratedPlanDraftV1`, ensambla `CanonicalPlanV1` desde snapshot congelado, crea `Document` + `DocumentVersion(validated)` append-only y enlaza `AiExecution.resulting_version_id`.
  - Reintento del mismo payload es idempotente por `source_payload_hash`; payload distinto no reemplaza historial. Provider/model/costo real son opcionales y quedan `null` si se desconocen.
  - Prepara `AiExecution(stage=audit,status=pending)` con PromptVersion audit activa y materializa `GENERACION_IA → AUDITORIA_IA`; 4C no ejecuta auditoría ni llama API.
  - Migración `2026_09_19_000001_create_documents_and_generation_import.php`: FK circulares de documentos/ejecución, inmutabilidad de versiones y constraint triggers diferidos para impedir `AUDITORIA_IA` sin resultado generation/documento/audit execution coherentes.
  - Verificación local: `ManualGenerationResultImportTest` **12 / 60**, `PlanningGenerationResultIntegrityTest` **5 / 19**, `PlanningGenerationIntegrityTest` **3 / 16**, `CanonicalPlanAssemblerTest` **11 / 22**, `AiExecutionContractTest` **9 / 21**, `PlanningCommercialIntegrityTest` **44 / 146** y suite completa **373 tests / 1308 assertions**, todos sin fallos. `git diff --check` limpio.

- [x] **Subfase 4D — auditoría manual estructurada.** Commit `3442f00`, tag `phase-4d-complete`.
  - Valida que el PromptVersion audit congelado use schema exacto `AuditResultV1`; la importación generation crea outbox `planning.audit.requested` en la misma transacción que `AUDITORIA_IA`.
  - `ai:process-outbox` materializa paquete privado `manual_audit` sobre `DocumentVersion`/hash exactos y deja audit execution `waiting_manual`; fallos abren `ai_failed` con `stage=audit`.
  - `ai:import-audit-result` valida pass/fail, categorías/severidades/JSON Pointer, guarda `audit_report` e idempotentemente marca audit `succeeded`. La solicitud permanece `AUDITORIA_IA`; routing queda para 4E.
  - Migración `2026_09_20_000001_add_manual_audit_pipeline.php`: shape/immutability de audit report y constraint triggers para exigir outbox/paquete/auditoría succeeded antes de estados posteriores.
  - Verificación local tras hotfix de precedencia: `AuditResultValidatorTest` **7 / 16**, `ManualAuditPipelineTest` **5 / 31**, `ManualAuditResultImportTest` **5 / 22**, `PlanningAuditIntegrityTest` **4 / 7**, `PlanningCommercialIntegrityTest` **44 / 146** y suite completa **394 tests / 1384 assertions**, todos sin fallos. `git diff --check` limpio.

- [x] **Subfase 4E — ruteo post-auditoría y corrección interna manual.** Commit `1552e43`, tag `phase-4e-complete`.
  - Audit pass crea Approval AI exacta y enruta a `APROBADA` (solo IA) o `REVISION_HUMANA` (plan revisado); la reserva humana no se consume en 4E.
  - Audit fail corregible crea execution/outbox correction y `AUDITORIA_IA → CORRECCION_IA`; `CorrectionResultV1` solo permite patch de secciones mutables y el servidor recompone `CanonicalPlanV1` preservando currículo/contexto/source.
  - La corrección crea `DocumentVersion` hija y siempre prepara reauditoría (`CORRECCION_IA → AUDITORIA_IA`). Alcance inseguro o máximo interno abre `ai_quality_attention`.
  - Los ciclos internos usan `AI_INTERNAL_CORRECTION_MAX_ROUNDS`/presupuesto técnico; **no gastan `correction_limit_snapshot` ni `client_correction`**, que siguen siendo rondas comerciales post-entrega.
  - PostgreSQL agrega `approvals` append-only, manifest correction y guardas diferidas para impedir ruteo/corrección/aprobación inconsistentes; aprobación humana permanece bloqueada hasta Fase 5.
  - Verificación local tras hotfix de validator/PLpgSQL: `CorrectionResultValidatorTest` **7 / 9**, `PlanningCorrectionIntegrityTest` **7 / 12**, `AuditRoutingTest` **9 / 36**, `ManualCorrectionPipelineTest` **3 / 17**, `ManualCorrectionResultImportTest` **8 / 30**, `PlanningAuditIntegrityTest` **4 / 8**, `PlanningCommercialIntegrityTest` **44 / 146** y suite completa **428 tests / 1489 assertions**, todos sin fallos. `git diff --check` limpio.

- [x] **Subfase 4F — cierre integral end-to-end.** Verificada sobre `1552e43`; no añade funcionalidad productiva.
  - `Phase4EndToEndTest` recorre 4B→4E como un único flujo: aprobación solo IA, cola `REVISION_HUMANA` sin consumo anticipado, correction→reaudit→approval sobre una nueva `DocumentVersion` y replays idempotentes sin duplicar versiones, approvals, outbox ni consumo.
  - Verificación local: `Phase4EndToEndTest` **4 tests / 32 assertions**, `PlanningCommercialIntegrityTest` **44 / 146** y suite completa **432 tests / 1521 assertions**, todos sin fallos. `git diff --check` limpio.
- [ ] Completar máquina de estados/bloqueos/eventos para revisión humana y etapas posteriores.
- [x] PromptTemplate/PromptVersion, contratos IA y modo manual de preparación de paquete (4A/4B).
- [x] Importar generación y persistir Document/DocumentVersion de forma verificada (4C).
- [x] Auditoría cerrada (4D) y corrección interna por sección verificada (4E).
- [x] Mapper de estados `/app` sin metadatos de proveedor/prompts/tokens/ejecuciones para los estados ya declarados.
- [x] Probar ambos itinerarios, saltos ilegales, duplicados y versiones inmutables; cobertura distribuida entre 4B–4F, con cierre vertical en `Phase4EndToEndTest`.

Salida: recorrido reproducible con fake/manual hasta aprobación automática o cola de revisión.

## Fase 5 — Calidad

- [x] **Subfase 5A — capacidad y asignación humana.** Cerrada en `365301a` / `phase-5a-complete`.
  - `reviewer_profiles`, `reviewer_grades`, `reviewer_availability` y `review_assignments` materializan capacidad en unidades, tarifa snapshot, grado+versión curricular exactos, ventana de disponibilidad e historial de reasignación.
  - `AssignReviewer` se ejecuta al entrar a `REVISION_HUMANA`: bloquea solicitud/perfiles en orden determinista, calcula carga activa y diaria, consume una sola vez la reserva `human_review` solo si existe candidato y crea una única asignación activa. Sin capacidad conserva la reserva y abre `no_reviewer`.
  - `ReassignReviewer` exige admin+motivo, conserva ciclo/historial, toma nueva tarifa snapshot y no vuelve a consumir cupo humano. Si no hay reemplazo viable conserva la asignación actual y abre `no_reviewer`.
  - PostgreSQL protege una asignación activa por solicitud, identidad/historial, grado exacto, disponibilidad, rol/estado del revisor, consumo humano, aprobación AI vigente, tarifa/unidades y límites `max_load`/`daily_max`.
  - Evidencia local: `ReviewerAssignmentTest` 10/46, `ReviewerAssignmentIntegrityTest` 8/16, concurrencia real 1/9; regresiones 4A–4F/comerciales verdes; suite **451 tests / 1592 assertions**.
- [x] **Subfase 5B — ejecución de revisión y checklist versionado.** Verificada sobre `365301a`; cierre etiquetado como `phase-5b-complete`.
  - Bootstrap `standard` v1 con 15 criterios obligatorios y tablas `review_checklist_versions`, `review_checklist_items`, `reviews`, `review_checklist_responses`.
  - `StartHumanReview` congela `DocumentVersion` + checklist publicado sobre la asignación activa; `SaveHumanReview` guarda respuestas y comentarios por sección; `ApproveHumanReview` exige todos los obligatorios en `true`, crea `Approval(kind=human)`, completa la asignación y mueve `REVISION_HUMANA → APROBADA` con evento exacto.
  - PostgreSQL vuelve verificable `approvals.review_id`, congela checklist publicado/review histórico/respuestas terminales y evita aprobación humana fabricada o sin evento de estado.
  - `/review` agrega recurso de revisiones asignadas con versión canónica mínima, inicio, guardado de checklist y aprobación; no expone pagos, prompts ni datos de cuenta del cliente.
  - Evidencia local: `ReviewChecklistVersionTest` **3 / 8**, `HumanReviewExecutionTest` **7 / 29**, `HumanReviewIntegrityTest` **5 / 5**; regresiones 5A, Fase 4 y hardening comercial verdes; rutas Filament `/review` registradas; suite completa **466 tests / 1634 assertions**, sin fallos. `git diff --check` limpio.
- [x] **Subfase 5C — decisiones del revisor y ciclo de corrección.** Verificada sobre `a206466`.
  - `changes_requested` exige checklist con al menos un criterio obligatorio fallido y comentarios por sección en alcance mutable; criterios curriculares/contextuales congelados fuerzan escalamiento en vez de autocorrección.
  - La corrección humana crea `AiExecution(stage=correction, source_kind=human_review)` + outbox, termina la asignación actual sin nuevo consumo de `human_review`, conserva el mismo ciclo comercial y usa el pipeline manual existente para producir una hija de `DocumentVersion` y reauditarla.
  - Cuando la reauditoría aprueba, `AssignReviewer` intenta devolver el trabajo al revisor anterior si continúa elegible/capaz; se crea nueva asignación del mismo ciclo y una nueva `Review` sin respuestas heredadas.
  - `escalated` y `rejected` son decisiones terminales de review que cancelan la asignación y abren `human_review_attention`; no cancelan la solicitud ni permiten reasignación automática mientras el bloqueo siga abierto.
  - PostgreSQL extiende la integridad de reviews y del manifest de corrección para distinguir origen `audit` vs `human_review`; no se debilita el alcance inmutable de currículo/contexto.
  - Evidencia local: `HumanReviewDecisionTest` **8 / 41**, `HumanReviewDecisionIntegrityTest` **4 / 5**; regresiones 5A/5B, ruteo/corrección de Fase 4 y hardening comercial verdes; suite completa **478 tests / 1680 assertions**, sin fallos. `git diff --check` limpio.
- [x] **Subfase 5D — honorarios, liquidaciones y métricas del revisor.** Verificada sobre `0570f84`.
  - `ApproveHumanReview` materializa un `reviewer_work_item` único por `request_id + cycle` usando exclusivamente `rate_snapshot_minor × units_snapshot` de la asignación final aprobada; cambios posteriores de tarifa, reintentos y correcciones del mismo ciclo no crean otro honorario.
  - `reviewer_settlements` agrupa trabajos aprobados del mismo revisor+moneda; administración aprueba y marca pago con referencia idempotente. El pago actualiza todos los trabajos del settlement en la misma transacción.
  - PostgreSQL congela identidad/monto de trabajos, impide mezclar revisor o moneda, exige review+asignación+Approval humana coherentes y vuelve inmutables trabajo/liquidación pagados.
  - `ReviewerMetricsService` expone carga activa, trabajos por liquidar/pagados y montos por moneda; `/review` muestra solo métricas propias, sin margen ni costos del cliente.
  - Evidencia local: `ReviewerCompensationTest` **9 / 45**, `ReviewerCompensationIntegrityTest` **4 / 6**; regresiones 5A–5C y hardening comercial verdes; suite completa **491 tests / 1731 assertions**, sin fallos. `git diff --check` limpio.
- [x] Probar checklist incompleto, asignación concurrente, reasignación/cancelación operativa y pago único por trabajo; cobertura distribuida entre 5A–5D.

Salida: revisión exacta por versión sin reescribir documentos completos ni exponer datos comerciales.

## Fase 6 — Documentos y retención

- [x] **Subfase 6A — archivos privados y catálogo de formatos.** Cerrada sobre base `d7ea7de` con PostgreSQL local.
  - Crear `files`, `institutional_formats`, `format_versions` y `document_version_files`; conectar FK reales pendientes en grupos, solicitudes y ejecuciones IA.
  - Publicar `standard-v1` idempotente y resolver formato por prioridad solicitud → perfil → estándar.
  - Publicación institucional requiere fuente limpia, mapping no vacío, muestra/validación aprobada y actor administrador; versión publicada queda inmutable.
  - PostgreSQL impide preferir/seleccionar formatos institucionales de otro propietario y deja preparado el vínculo seguro de archivos resultantes.
  - Evidencia local: `FormatFoundationTest` **10 / 20**; regresiones relacionadas verdes; suite completa **501 tests / 1751 assertions**, sin fallos. `git diff --check` limpio.
- [x] **Subfase 6B — renderer estándar DOCX/PDF y pipeline de render.**
  - `DispatchDocumentRendering` y `RenderPlanningDocument` ejecutan `APROBADA → GENERANDO_DOCUMENTO → LISTA_PARA_ENTREGAR` de forma idempotente sobre la `DocumentVersion` actual y el `FormatVersion` resuelto.
  - El renderer estándar genera DOCX y PDF reales, los guarda en storage privado y los vincula mediante `document_version_files`; un fallo mantiene el run recuperable y no inventa archivos.
  - PostgreSQL exige autorización comercial, versión actual, `document_render_run` coherente y artefactos reales antes de aceptar estados de render/entrega; el hardening documental no oculta `AUTHORIZATION_REQUIRED` de Fase 3.
  - Evidencia local: `DocumentRenderingTest` **9 / 63**, `DocumentRenderingIntegrityTest` **4 / 8**; regresiones críticas verdes; suite completa **514 tests / 1822 assertions**, sin fallos, ejecutada dos veces. `git diff --check` limpio.
- [ ] Flujo institucional de análisis/mapping/muestra/publicación con renderer explícito.
- [x] **Subfase 6C — entrega privada, historial y retención.** Cerrada sobre `2569b3d`; tag `phase-6c-complete`.
  - `PublishPlanningDelivery` congela `request + DocumentVersion + DocumentRenderRun + DOCX/PDF`, crea `delivery_files`, extiende retención configurable y materializa `LISTA_PARA_ENTREGAR → ENTREGADA`.
  - Descarga por controlador autenticado y pertenencia exacta; cada descarga crea historial append-only, sin exponer storage público ni enlaces permanentes.
  - `documents:purge-expired` elimina únicamente bytes vencidos y conserva `files`, `deliveries`, `delivery_files` y `delivery_downloads` para trazabilidad.
  - PostgreSQL impide entregar sin versión/run/archivos/evento coherentes, acortar retención o mutar historial de entrega.
  - Evidencia local: `PrivateDeliveryTest` **8 / 55**, `PrivateDeliveryIntegrityTest` **4 / 13**; suite completa **526 tests / 1890 assertions**, sin fallos. `git diff --check` limpio.
- [ ] **Subfase 6D — corrección del cliente por ventana/cuota conservando entrega anterior.** Candidato en `phase-6d-client-corrections` sobre `b5e141b`.
  - Materializar `correction_requests` con una sola corrección abierta por solicitud, historial terminal inmutable y vínculo explícito a la versión entregada/fuente.
  - Solicitud del docente valida ownership, entrega vigente, ventana contractual congelada y rondas disponibles; retirar o rechazar vuelve al estado previo sin consumir ronda.
  - Aceptación administrativa reserva/consume `client_correction=1` contra el periodo histórico aunque la suscripción ya haya expirado, y entra al pipeline existente como `source_kind=client`.
  - El resultado crea `DocumentVersion` hija, resuelve la solicitud de corrección y obliga `CORRECCION_IA → AUDITORIA_IA`; la entrega anterior permanece disponible y una entrega posterior será independiente.
  - Añadir acciones `/app` y administración más pruebas funcionales/de integridad PostgreSQL antes de marcar 6D cerrada.
- [ ] Notificaciones internas/email y renovación próxima deduplicadas.
- [ ] Probar render fallido, formato pendiente, descarga ajena, corrección tras expiración y email fallido.
- [ ] Verificar visualmente DOCX/PDF de muestras y navegación cliente/revisor.

Salida: entrega y corrección descargables, trazables y visualmente válidas.

## Fase 7 — Automatización real

- [ ] Elegir e integrar un proveedor IA; presupuesto, timeouts, schema y costos.
- [ ] Elegir gateway real; sandbox, firma, conciliación y reembolso idempotente.
- [ ] Probar timeout ambiguo, evento repetido/desordenado y caída/reinicio de worker.
- [ ] Verificar contingencia manual explícita y datos minimizados.

Salida: pipeline API y compra autoservicio reales verificados en sandbox; no declarar listo antes.

## Fase 8 — Operación y piloto

- [ ] Dashboard de atención con enlaces de resolución, ventas, IA y revisores.
- [ ] Margen ponderado por unidades y conciliación de asignaciones/costos/devoluciones, centavos y restituciones.
- [ ] Suite completa: permisos, ownership, estados, límites, consumo, asignación, revisión, versiones, correcciones, pagos y pipeline.
- [ ] Build de assets y recorrido responsive/teclado con datos ficticios en ambos planes.
- [ ] Configurar workers/scheduler, backup y restauración probada.
- [ ] Definir políticas comerciales, retención, proveedores y calidad antes de venta.
- [ ] Medir tiempos y automatización contra línea base; documentar limitaciones.

Salida: evidencia de pruebas, restauración, calidad documental y costos; publicación fuera de esta entrega.

## Regla de actualización

En cada fase registrar fecha, archivos cambiados, comandos/pruebas ejecutados, resultados, revisión visual cuando aplique y limitaciones. Marcar solo verificaciones ejecutadas. Actualizar este archivo durante todo el desarrollo; no convertir un diseño propuesto en prueba aprobada.

### Evidencia de revisión documental — 2026-09-07

Se verificó existencia y contenido de los siete documentos, equilibrio de bloques Markdown y correspondencia de los nombres de estado/entidades entre flujos, datos y pipeline. Se revisaron manualmente las ramas IA/revisada, corrección con nueva auditoría, consumo idempotente y aislamiento tras reasignación. Los documentos son archivos nuevos sin seguimiento en Git; git diff --check no valida su contenido por sí solo. No se ejecutaron tests de aplicación, migraciones ni instalación de dependencias.


## Iteración funcional — diseño terminado, sin código

- [x] Mantener monolito/tres paneles y adoptar PostgreSQL como única base objetivo.
- [x] Diseñar catálogo curricular versionado, inmutabilidad del árbol, pivotes y snapshot textual en CURRICULUM.md.
- [x] Diseñar seeders ficticios, sin crear scripts ni cargar información curricular real.
- [x] Diseñar NUEVA PLANEACIÓN rápido/avanzado, servicio determinista desacoplado y perfil/formato reutilizados.
- [x] Definir unidad calendar_days_v1, consumo proporcional, correcciones por ronda, revisión/carga/honorarios ponderados.
- [x] Clasificar adiciones MVP/POST-MVP sin ampliar fases mayores; conservar límites de formatos.

Documentos afectados: ARCHITECTURE.md, DATABASE.md, WORKFLOWS.md, AI_PIPELINE.md, MVP.md, TASKS.md y nuevo CURRICULUM.md. PERMISSIONS.md y archivos históricos se mantienen intactos. Las tareas técnicas anteriores siguen pendientes.

### Migraciones que habrá que crear (no creadas ahora)

1. Catálogo: curricula, curriculum_versions, educational_phases, grades, formative_fields, curricular_contents, pdas, articulating_axes; claves compuestas, publicación y triggers de inmutabilidad. selectable_version_id después de curriculum_versions.
2. Grupos/perfil: curriculum_version_id/grade_id y preferred_format_id; agregar FK de formato después de crear formatos. reviewer_grades para autorizaciones por versión.
3. Solicitudes: versión curricular, grado, modo, revisión/confirmación y evaluación sugerida; request_curricular_contents, request_pdas y request_articulating_axes; snapshot textual en request_input_versions. curriculum_suggestions con fingerprint y estrategia.
4. Comercial: cuatro límites PlanVersion, planning_days/planning_units/commercial_calculation_snapshot, planning_request_segments y reservas con cantidades. No dejar correction_limit_per_request duplicado.
5. Calidad/finanzas: units_snapshot, tarifa por unidad/total en asignación y trabajo; units y ordinales de asignación de ingreso. Mantener historial/idempotencia existentes.
6. PostgreSQL: tipos timestamptz/JSONB/NUMERIC/bigint, CHECK y FK; índices únicos parciales de suscripción vigente, asignación activa y corrección abierta; completar FK circulares al final.

Como no hay aplicación ni migraciones iniciales, incorporar estos campos al crear tablas; no escribir ALTER ni importación MySQL innecesarios. Mantener las demás migraciones base ya planeadas (usuarios, pagos, documentos, jobs, etc.). Fase 1 fija versión PostgreSQL y prueba conexión/extensiones; Fases 2–6 materializan las extensiones correspondientes.

### Verificación documental de esta iteración

Se revisaron referencias a MySQL, correction_limit_per_request, cupo humano fijo y captura curricular manual: solo quedan menciones históricas o explicaciones de sustitución. Se verificaron los siete documentos afectados, enlaces locales existentes y bloques Markdown equilibrados mediante revisión estática. Se contrastaron además consumo U, carga/honorarios y corrección sin doble cobro entre documentos. No se han ejecutado tests de aplicación, migraciones ni seeders; PostgreSQL es una decisión de diseño todavía no provisionada. La próxima acción de implementación requiere un nuevo turno autorizado: NO comenzar Fase 1 en esta entrega.

### Ajuste documental — importación estructurada

- [x] Diferenciar importación administrativa JSON/CSV (MVP/Fase 2) de sincronización, PDF inteligente, scraping y procesamiento automático (POST-MVP).
- [x] Diseñar servicio/acción/comando, schema, transacción, dry-run, resumen y errores; crear solo borrador con publicación editorial independiente.

Solo CURRICULUM.md, MVP.md y TASKS.md afectados por este ajuste. No requiere nuevas tablas: usa catálogo y auditoría existentes. No se implementó el comando, no se importaron datos y no se inició Fase 1; las verificaciones de ejecución de Fase 2 permanecen pendientes.
